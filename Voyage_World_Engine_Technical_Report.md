# Voyage World Engine 技术架构深度报告

> **报告日期**: 2026年8月3日  
> **研究对象**: Latitude 公司旗下 AI 原生 RPG 平台 Voyage 的核心引擎 —— World Engine  
> **信息来源**: 公开技术博客、官方发布资料、创始人访谈及行业报道

---

## 一、摘要

Voyage 是由 Latitude 公司（AI Dungeon 开发商）于 2026 年 4 月推出的**全球首款 AI 原生 RPG 平台**。其核心驱动力为自研的 **World Engine（世界引擎）**——一个历时 5 年、历经 6 个原型迭代的多 AI 协作确定性系统。World Engine 的设计目标是解决传统 LLM 在开放世界 RPG 中的根本缺陷：**幻觉、状态丢失、逻辑不一致和缺乏持久性**。它通过将"硬状态"与"软叙事"分离，构建了一个能够跨数千回合保持一致的动态虚拟世界。

---

## 二、架构总览

### 2.1 设计哲学

World Engine 的核心设计哲学可概括为：**"AI 负责叙事，引擎负责真相"**。

传统 LLM（如 GPT 系列）存在"讨好式即兴创作"（Agreeable Improvisation）问题——玩家说什么模型都迎合，容易遗忘已建立的设定、凭空生成物品或角色。World Engine 通过在底层 LLM 之上构建一个**确定性规则执行层**，充当"公正的 Game Master"，确保 AI 不能幻觉掉已确立的事实。

### 2.2 系统拓扑

```
┌─────────────────────────────────────────────────────────────┐
│                      Voyage World Engine                     │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐   │
│  │  叙事引擎     │  │ 机制解析引擎  │  │ 世界状态追踪器    │   │
│  │ (Narrative)  │  │ (Mechanics)  │  │ (World State)    │   │
│  └──────┬───────┘  └──────┬───────┘  └────────┬─────────┘   │
│         │                 │                    │             │
│  ┌──────┴─────────────────┴────────────────────┴─────────┐   │
│  │              确定性状态管理层                        │   │
│  │     (Deterministic State Management Layer)           │   │
│  └────────────────────────┬─────────────────────────────┘   │
│                           │                                  │
│  ┌────────────────────────┴─────────────────────────────┐   │
│  │              角色记忆与关系系统                         │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │   │
│  │  │ 短期记忆     │  │ 长期记忆     │  │ 关系图谱     │   │   │
│  │  │ (Session)   │  │ (Persistent)│  │ (Social)    │   │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘   │   │
│  └────────────────────────────────────────────────────────┘   │
│                           │                                  │
│  ┌────────────────────────┴─────────────────────────────┐   │
│  │              多模型协作层                             │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │   │
│  │  │ Latitude    │  │ Google      │  │ Google      │   │   │
│  │  │ 自研模型    │  │ Gemini Flash│  │ Gemma       │   │   │
│  │  │ (核心逻辑)  │  │ (图像生成)  │  │ (文本/音频) │   │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘   │   │
│  └────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、核心子系统详解

### 3.1 叙事引擎 (Narrative Engine)

**职责**: 生成场景描述、动作结果、NPC 对话、环境氛围文本。

**技术特点**:
- 接收玩家输入的自然语言指令
- 结合世界状态追踪器提供的当前状态，生成上下文一致的叙事
- 受机制解析引擎约束——不能生成违反物理规则或游戏机制的结果
- 支持多模态输出：文本、音频叙述、AI 生成图像

**约束机制**: 叙事引擎不是"自由创作"，而是**在确定性状态框架内的创意填充**。例如，如果世界状态追踪器记录玩家当前 HP 为 0，叙事引擎不能生成"你站起来继续战斗"的描述。

### 3.2 机制解析引擎 (Mechanics Engine)

**职责**: 处理技能检定、战斗判定、概率事件、数值计算。

**技术特点**:
- 类似桌面 RPG 的掷骰系统，但完全由 AI 驱动
- 解析玩家输入中的意图，映射到游戏机制（如"尝试潜行"→ 敏捷检定）
- 计算伤害、经验值、技能升级等数值变化
- 将结果反馈给世界状态追踪器进行状态更新

**与叙事引擎的协作**: 当玩家执行攻击动作时，机制解析引擎计算命中概率和伤害值，叙事引擎则根据这些数值生成富有戏剧性的战斗描述。两者并行工作，确保"规则正确"与"叙事精彩"兼得。

### 3.3 世界状态追踪器 (World State Tracker)

**职责**: 管理所有"硬事实"（Hard Facts），防止 AI 幻觉。

**追踪的状态类别**:

| 状态类别 | 具体内容 | 持久性 |
|---------|---------|--------|
| **角色状态** | HP、MP、技能等级、特殊能力、经验值 | 跨会话持久 |
| **物品与经济** | 库存清单、装备栏、货币数量、物品属性 | 跨会话持久 |
| **地理信息** | 角色在世界地图中的精确坐标、已探索区域 | 跨会话持久 |
| **任务进度** | 主线/支线任务完成状态、关键决策节点 | 跨会话持久 |
| **世界事件** | 时间流逝、季节变化、重大事件历史 | 全局持久 |

**技术实现**: 采用结构化数据存储（推测为图数据库或关系型数据库 + 缓存层），与 LLM 的生成输出解耦。LLM 在生成叙事前，必须查询状态追踪器获取当前事实；生成后，状态追踪器验证并更新状态。

### 3.4 角色记忆系统 (Character Memory System)

**职责**: 维护每个 NPC 的完整认知档案。

**记忆分层架构**:

```
┌─────────────────────────────────────────┐
│           NPC 记忆架构                   │
├─────────────────────────────────────────┤
│  第一层: 核心身份 (Core Identity)        │
│  - 背景故事、种族、职业、性格特质         │
│  - 永久不变，定义角色本质                 │
├─────────────────────────────────────────┤
│  第二层: 长期记忆 (Long-term Memory)    │
│  - 跨会话的关键事件、重大决策              │
│  - 与玩家的历史互动摘要                   │
│  - 关系变化里程碑                        │
├─────────────────────────────────────────┤
│  第三层: 短期记忆 (Short-term Memory)   │
│  - 当前会话内的对话上下文                  │
│  - 近期发生的事件                        │
│  - 当前情绪状态                          │
├─────────────────────────────────────────┤
│  第四层: 工作记忆 (Working Memory)      │
│  - 当前场景中的即时感知                   │
│  - 正在进行的交互对象                     │
│  - 即时目标与计划                         │
└─────────────────────────────────────────┘
```

**记忆压缩与检索**: 面对数千回合的交互历史，系统需要智能地压缩旧记忆、提取关键事实。推测采用以下技术：
- **摘要生成**: 定期将长对话历史压缩为结构化摘要
- **关键事件标记**: 标记背叛、结盟、重大交易等"不可遗忘"事件
- **向量检索**: 使用 Embedding 模型检索与当前上下文相关的历史记忆

### 3.5 关系与后果引擎 (Relationship & Consequence Engine)

**职责**: 追踪并计算玩家行为对世界产生的长期连锁反应。

**核心功能**:
- **关系图谱**: 维护角色之间的社交关系网络（好感度、信任度、敌对度、债务关系）
- **后果传播**: 某个行为的影响不仅限于直接对象，还会通过网络传播（例如：你杀死了 A，A 的兄弟 B 会因此敌视你，B 的朋友 C 可能拒绝与你交易）
- **声誉系统**: 在特定区域或阵营中的声誉值，影响 NPC 的初始态度
- **因果链追踪**: 记录"因为 X 所以 Y"的因果逻辑，确保 AI 在后续叙事中尊重这些因果关系

---

## 四、确定性状态管理 (Deterministic State Management)

这是 World Engine 区别于普通 LLM 应用的最关键技术。

### 4.1 问题定义

LLM 的生成过程本质上是**概率性的**（Probabilistic）。给定相同的提示，模型可能输出不同的结果。这在创意写作中是优势，但在 RPG 中是致命缺陷：
- 玩家上次有 100 金币，这次 AI 说"你身无分文"
- NPC 上次答应帮忙，这次 AI 说"我们从未见过"
- 玩家击败了 Boss，下次 AI 让 Boss 再次出现

### 4.2 解决方案：双轨架构

World Engine 采用**双轨架构**解决此问题：

| 轨道 | 性质 | 职责 | 技术实现 |
|------|------|------|---------|
| **状态轨道** | 确定性 (Deterministic) | 存储和验证所有客观事实 | 结构化数据库、规则引擎 |
| **叙事轨道** | 概率性 (Probabilistic) | 生成描述性文本和对话 | LLM（Latitude 自研模型） |

**工作流程**:
1. 玩家输入指令
2. 机制解析引擎根据当前状态计算结果
3. 状态轨道更新客观事实（HP 变化、物品增减、位置移动）
4. 叙事轨道接收"状态变化"和"上下文"，生成叙事文本
5. 状态轨道验证叙事文本是否与事实冲突（如有冲突则修正或拒绝）

### 4.3 防幻觉机制

- **事实注入 (Fact Injection)**: 在 LLM 的 prompt 中显式注入当前状态事实，强制模型遵守
- **输出验证 (Output Validation)**: 使用规则引擎或小型验证模型检查 LLM 输出是否与状态一致
- **状态锁定 (State Locking)**: 关键状态（如死亡、任务完成）一旦确立，不可被 LLM 覆盖

---

## 五、多模型协作架构

Voyage 不依赖单一 LLM，而是采用**多模型协作**策略：

| 模型/系统 | 提供商 | 用途 | 选择理由 |
|-----------|--------|------|---------|
| **核心推理模型** | Latitude 自研 | 叙事生成、逻辑推理、NPC 行为决策 | 针对 RPG 场景微调，理解游戏机制 |
| **图像生成模型** | Google Gemini Flash | 场景插画、角色肖像、物品图标 | 高质量、低延迟的图像生成 |
| **多模态基础模型** | Google Gemma | 文本理解、音频生成、视频相关 | 开源、可定制、多模态能力 |

**协作模式**:
- **管道式 (Pipeline)**: 核心模型生成文本 → Gemma 生成音频/辅助理解 → Gemini Flash 生成配图
- **并行式 (Parallel)**: 多个模型同时处理不同子任务（如一个模型处理战斗机制，一个处理 NPC 对话）
- **裁决式 (Arbitration)**: 当模型之间产生冲突时，由规则引擎或更高层模型进行裁决

---

## 六、NPC 自主智能体架构

Voyage 的 NPC 不是传统 RPG 中的"对话树"或"脚本化行为"，而是**自主智能体 (Autonomous Agents)**。

### 6.1 智能体架构

```
┌─────────────────────────────────────────┐
│           NPC 自主智能体                 │
├─────────────────────────────────────────┤
│  感知层 (Perception)                     │
│  - 观察周围环境、玩家行为、其他 NPC 动作   │
├─────────────────────────────────────────┤
│  认知层 (Cognition)                      │
│  - 记忆检索、目标规划、情绪评估            │
│  - 内部动机系统（生存、财富、复仇、忠诚）  │
├─────────────────────────────────────────┤
│  决策层 (Decision)                       │
│  - 基于当前状态和目标选择行动               │
│  - 考虑关系网络和长期后果                   │
├─────────────────────────────────────────┤
│  执行层 (Action)                         │
│  - 执行选定的行动（移动、对话、战斗、交易）  │
│  - 更新自身状态和世界状态                   │
└─────────────────────────────────────────┘
```

### 6.2 自主行为示例

- **记仇**: 如果玩家背叛了 NPC，该 NPC 可能在数小时甚至数天后组织报复
- **联盟动态**: NPC 之间可以自行结盟或敌对，不受玩家直接控制
- **目标追求**: 一个贪婪的商人 NPC 会主动寻找交易机会，即使玩家不在场
- **信息传播**: NPC 之间会传递信息（如"那个冒险者偷了东西"），影响玩家声誉

### 6.3 世界离线演化

当玩家下线后，World Engine 仍在后台运行：
- NPC 继续执行其目标和计划
- 世界事件按时间线推进
- 玩家再次上线时，世界已经发生了变化

这要求引擎具备**时间压缩**和**事件调度**能力，在玩家不在场时高效模拟世界演化。

---

## 七、持久化与跨会话机制

### 7.1 持久化层级

| 层级 | 范围 | 存储内容 |
|------|------|---------|
| **玩家档案** | 全局 | 账号信息、订阅状态、偏好设置 |
| **角色存档** | 每角色 | 属性、技能、装备、任务进度 |
| **世界存档** | 每世界 | 世界状态、NPC 状态、历史事件 |
| **会话状态** | 每次游玩 | 当前场景、临时变量、缓存数据 |

### 7.2 跨会话一致性挑战

在数千回合的交互后，如何保持 NPC"记得"玩家？

**技术策略**:
1. **结构化摘要**: 将会话历史压缩为关键事实列表，而非存储原始对话
2. **情感标签**: 为每次交互打上情感标签（友好、敌对、中立），简化记忆检索
3. **关系快照**: 定期保存角色关系图谱的快照，作为后续会话的上下文
4. **渐进式遗忘**: 旧记忆的权重逐渐降低，但关键事件永久保留

---

## 八、技术演进历程

World Engine 并非一蹴而就，而是经历了长期迭代：

| 阶段 | 时间 | 里程碑 |
|------|------|--------|
| **原型 1-3** | 2021-2023 | 基于 GPT-3/GPT-4 的实验性 RPG 系统，验证 AI 叙事可行性 |
| **原型 4** | 2023-2024 | 引入状态管理概念，解决 AI Dungeon 的幻觉问题 |
| **原型 5** | 2024-2025 | 多 AI 系统协作架构确立，NPC 自主行为初步实现 |
| **原型 6** | 2025-2026 | 完整 World Engine 成型，持久化世界和离线演化实现 |
| **Voyage 发布** | 2026.04 | 扩大测试 (Expanded Beta) 启动 |

---

## 九、性能与扩展性考量

### 9.1 计算成本

多 AI 系统并行 + 状态管理 + 持久化存储，意味着高昂的计算成本：
- 每个玩家动作可能触发 3-5 个 AI 调用
- 状态验证和记忆检索增加延迟
- 世界离线演化需要后台计算资源

**应对策略**:
- 分层订阅制（$15/$30/$50）对应不同 AI 调用配额
- 基础版免费但限制行动次数
- 推测使用缓存和批处理优化后台演化

### 9.2 扩展性挑战

- **并发世界**: 如何支持数百万玩家同时运行各自的世界？
- **共享世界**: 多人协作时，如何同步不同玩家的世界状态？
- **长期存档**: 数千回合的数据如何高效存储和检索？

---

## 十、总结与行业启示

### 10.1 World Engine 的核心创新

1. **确定性 + 概率性双轨架构**: 首次在消费级 AI 游戏中实现"硬状态"与"软叙事"的分离
2. **多 AI 协作**: 打破单一 LLM 包办一切的范式，让专业系统做专业的事
3. **持久化世界**: 将游戏从"会话"提升为"世界"，实现真正的动态演化
4. **自主 NPC**: 从"脚本化角色"到"智能体"的范式转变

### 10.2 对 AI 游戏开发的启示

- **不要完全信任 LLM**: 在关键状态上必须引入确定性约束
- **状态管理是核心**: 优秀的 AI 游戏引擎首先是一个优秀的状态管理系统
- **记忆设计至关重要**: 什么该记住、什么该遗忘、如何检索，决定了 NPC 的"智能感"
- **多模型是趋势**: 单一模型无法满足复杂游戏的所有需求

### 10.3 局限与待观察

- **计算成本**: 多 AI 系统 + 持久化世界的运营成本能否支撑免费模式？
- **一致性边界**: 在极端复杂的交互链中，确定性系统是否会出现逻辑漏洞？
- **玩家体验**: 完全开放的世界是否会导致"选择 paralysis"或叙事碎片化？

---

> **免责声明**: 本报告基于公开资料整理分析，部分技术细节（如具体的数据库选型、模型参数、内部 API 设计）为基于架构描述的合理推测，未经 Latitude 官方确认。

---

*报告完*
