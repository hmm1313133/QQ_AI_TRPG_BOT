# 世界编辑器与素材联动设计

> 状态：待实施（方案评审稿）
> 关联文档：《管理后台扩展设计.md》《世界设定库与按需加载设计.md》《AI世界引擎技术设计.md》
> 范围：管理后台「世界管理」→「世界编辑器」的全面结构化改版，含数据模型、素材联动、Prompt 注入、前端编辑器与创建向导。

## 一、背景与问题

### 1.1 现状

世界编辑器（`/admin/worlds/:id`）已按分区拆成页签（概览/设定库/场景/角色/地点势力/任务线索/注入记录/JSON），但存在三个层面的"粗糙"：

1. **字段不结构化**：
   - 角色（CharacterState）缺少玩家创作视角的核心字段——没有外貌、性格描述、背景故事、技能；现有的 `role/motivation/secrets` 是主持人视角字段，混在一起平铺成一排输入框。
   - 物品/道具**没有实体**，只能写进 Background 文本或 lore 条目，无法被追踪（在哪、谁拿着）。
   - 势力（Faction）只有 `name + reputation`，没有描述、目标、领袖。
   - 地点（Location）只有 `name + description + exits`，没有氛围、危险度、兴趣点。
2. **创建向导简陋**：simrpg 只能填"世界设定大文本 + 初始地点名单"；roleplay 的 NPC 只有名字+一句话性格。玩家想精细创作，只能创建后去 JSON 页签手改。
3. **素材零联动**：
   - 全局人物卡库（`data/characters`，`CharacterState.CardRef` 早已预留关联字段）与世界编辑器完全断开，没有任何"把已有角色卡带进这个世界"的入口。
   - 世界 A 精心捏的 NPC/地点无法复用到世界 B。
   - 剧本（trpg）里的角色定义是一份只读素材，不能被其他模式的世界引用。

### 1.2 目标

把「世界管理」升级为**自由可定制的世界组装编辑器**：

> 玩家写一段世界背景故事，然后像搭积木一样勾选/创建想要的素材——人物（含已有角色卡）、地点、物品道具、势力、导演系统主线剧情、设定条目——组成一个世界。

三个原则：

- **结构化优先**：能拆成字段的就不让玩家写大段文本；文本域留给真正的叙事内容（背景故事、外貌描写）。
- **素材可复用**：人物卡库、其他世界、剧本角色都是素材来源，一次创作到处使用。
- **不破坏硬状态原则**：结构化实体仍是 WorldState 硬状态（经 Engine 落库），lore 设定库仍是软叙事按需注入，两者职责不变、可互相桥接。

## 二、概念模型

```
世界（WorldState）
├── 背景故事 Background          （已有，恒定设定文本）
├── 主线剧情 Storyline           （新增，导演系统的故事脊柱，分幕推进）
├── 人物 Characters              （扩展：+外貌/性格/背景故事/技能）
│     └── CardRef → 全局人物卡    （联动：数值真相在人物卡，叙事状态在世界）
├── 地点 Locations               （扩展：+氛围/危险度/兴趣点）
├── 物品 Items                   （新增实体：类型/描述/所在/持有者）
├── 势力 Factions                （扩展：+描述/目标/领袖）
├── 任务 Quests / 线索 HiddenInfo（已有，不动）
└── 设定条目 Lore                （已有，软叙事按需注入）

素材来源（导入时复制/关联进世界）：
├── 全局人物卡库（data/characters）── CardRef 关联，数值随卡
├── 其他世界的实体                ── 复制（快照，此后各自演化）
└── 剧本角色（trpg 剧本模板）     ── 复制（同 InitFromScript 的播种逻辑）
```

联动语义区分两种：

- **关联（link）**：仅人物卡。世界角色存 `CardRef`，HP/SAN/技能数值以人物卡为真相，世界内只存叙事状态。这是已有设计的补完。
- **复制（copy）**：其余素材。导入即快照复制进世界，之后与原世界/剧本各自独立演化——避免跨世界写冲突，符合单一写入者原则。

## 三、数据模型扩展

全部新增字段带 `omitempty`，旧世界 JSON 零迁移成本；`applySection` 反序列化天然兼容。

### 3.1 CharacterState 扩展（internal/world/types.go）

```go
// 新增四个玩家创作字段（均为叙事软信息，不影响规则判定）
Appearance  string   `json:"appearance,omitempty"`  // 外貌描写
Personality string   `json:"personality,omitempty"` // 性格描述（长文；Traits 仍是短标签）
Backstory   string   `json:"backstory,omitempty"`   // 背景故事
Skills      []string `json:"skills,omitempty"`      // 能力/特长描述列表，如 "剑术(娴熟)"
```

同步扩展：

- `world.NPCSeed`（创建向导种子）加同名字段 + `CardRef string`（创建即关联人物卡）。
- 全局人物卡 `character.Card` 加 `Appearance`、`Personality` 两个可选字段（Backstory/Skills 已有），供 roleplay 模式与导入时映射。
- 人物卡 → 世界角色的导入映射：Card.Skills（map）按 `"技能名(数值)"` 拍平进 Skills；Backstory/Appearance/Personality 直拷；`CardRef = card.ID`。

### 3.2 新增 Item 实体

```go
// Item 世界内物品/道具（硬状态，可被追踪与转移）。
type Item struct {
    ID          string   `json:"id"`
    Name        string   `json:"name"`
    Type        string   `json:"type,omitempty"`        // weapon / consumable / key / material / other
    Description string   `json:"description,omitempty"`
    Location    string   `json:"location,omitempty"`    // 所在地点名（与 Location.Name 对应）
    Owner       string   `json:"owner,omitempty"`       // 持有者（角色名；"玩家" 表示玩家持有）
    Tags        []string `json:"tags,omitempty"`
}
```

`WorldState` 加 `Items map[string]*Item `json:"items,omitempty"``（键 = 名称，与 Locations/Factions 同构）。
物品的位置/归属变更是运行时高频操作，但**本期不做** LLM 事件类型（见 §六遗留），先支持管理端编辑 + 场景注入。

### 3.3 Faction / Location 扩展

```go
type Faction struct {
    ID, Name    string
    Reputation  int
    Description string   `json:"description,omitempty"` // 新增
    Goals       []string `json:"goals,omitempty"`       // 新增：势力目标
    Leader      string   `json:"leader,omitempty"`      // 新增：领袖（可对应角色名）
}

type Location struct {
    ID, Name, Description string
    Exits      []string
    Atmosphere string   `json:"atmosphere,omitempty"` // 新增
    Danger     string   `json:"danger,omitempty"`     // 新增
    Points     []string `json:"points,omitempty"`     // 新增：兴趣点/可调查处
}
```

### 3.4 新增 Storyline（导演主线）

```go
// Storyline 导演系统的主线剧情脊柱（simrpg/roleplay 手工编排；
// trpg 模式的主线在剧本 Timeline 里，不用此结构）。
type Storyline struct {
    Title   string     `json:"title"`
    Premise string     `json:"premise,omitempty"` // 主线前提/悬念
    Acts    []StoryAct `json:"acts,omitempty"`
}

type StoryAct struct {
    ID      string `json:"id"`
    Title   string `json:"title"`
    Summary string `json:"summary,omitempty"`
    Status  string `json:"status"` // pending / active / done
}
```

`WorldState` 加 `Storyline *Storyline `json:"storyline,omitempty"``。
幕状态推进：本期管理端手动编辑；LLM 自动推进（新事件类型 `storyline_advance`）列入遗留。

### 3.5 新分区

`sectionParts` 白名单与 `applySection` / `handleSectionGet` 新增：`items`、`storyline`（storyline 为单对象分区）。前端 JSON 页签的 `JSON_WRITABLE` 同步加这两项。

## 四、素材联动 API

### 4.1 素材目录

```
GET /api/admin/assets
→ {
    "library": [{ "id", "kind", "name", "tags", "summary", "source" }...],            // 全局素材库（§9.3）
    "cards":   [{ "id", "name", "system", "player", "has_backstory" }...],          // 全局人物卡
    "worlds":  [{ "id", "mode", "script_name",
                  "characters": ["名字"...], "locations": [...], "items": [...], "factions": [...] }...],
    "scripts": [{ "id", "name", "characters": [{ "name", "role" }...] }...]          // 剧本角色
  }
```

聚合自素材库表 / `CharManager` / `WorldEngine` / `Archive`，均为只读摘要，供前端素材选择器拉取。

### 4.1.1 素材库管理

```
GET    /api/admin/assets/library?kind=&q=&tag=   // 列表（kind/名称模糊/标签过滤）
POST   /api/admin/assets/library                 // 手动新建模板 {kind, name, tags, summary, payload}
GET    /api/admin/assets/library/{id}            // 详情（含 payload）
PATCH  /api/admin/assets/library/{id}            // 编辑
DELETE /api/admin/assets/library/{id}            // 删除
POST   /api/admin/worlds/{id}/assets/collect     // 收藏世界实体进素材库 {kind, name, tags?, summary?}
```

### 4.2 素材导入

```
POST /api/admin/worlds/{id}/assets/import
{
  "library": ["astID1", ...],                         // 复制：素材库模板 → 世界实体
  "cards": ["cardID1", ...],                          // 关联：人物卡 → CharacterState(CardRef)
  "copy":  [{ "world_id": "...", "kind": "character|location|item|faction", "name": "..." }, ...],
  "script_characters": [{ "script_id": "...", "name": "..." }, ...]
}
```

语义与校验：

- 世界锁内执行，全部校验通过才落库（避免半导入），完成后记 note 审计事件（`Actor=admin`）。
- 同名冲突：人物卡关联遇同名世界角色 → 补 CardRef 并合并缺失字段（不覆盖已有叙事状态）；copy 类遇同名 → 返回冲突清单，由前端提示改名或跳过（`"on_conflict": "skip|overwrite"` 参数，默认 skip）。
- 剧本角色复制复用 `InitFromScript` 中角色播种的映射逻辑（抽公共函数，避免双份实现漂移）。

### 4.3 创建向导请求扩展

`worldCreateReq` 扩展（全部可选）：

```go
Characters []world.CharacterState `json:"characters"` // 结构化 NPC（含新字段），与 NPCs 简易种子并存
Items      []world.Item           `json:"items"`
Factions   []world.Faction        `json:"factions"`
Storyline  *world.Storyline       `json:"storyline"`
ImportCards []string             `json:"import_cards"` // 创建即关联的人物卡
```

`SeedSpec` / `SeedWorld` 同步扩展，保持"创建经 Engine 单写入"原则。trpg 模式同样接受这些字段（剧本播种之上追加）。

## 五、Prompt 注入更新

`buildGameStateSummary`（internal/agent/narrator_prompt.go）在现有"在场 + lore 命中"角色清单基础上：

- **角色块**：对注入的每个角色，追加非空的 `Appearance / Personality / Skills / Backstory`，每个字段截断 ~80 字符防膨胀（完整内容始终可经 lore 条目按需召回——见 §5.1 桥接）。
- **物品块**：列出 `Location == 当前场景名` 或 `Owner == 玩家` 的物品（名称+类型+一句话描述），其余物品不进摘要。
- **主线块**：Storyline 存在时注入"主线：标题 + 前提 + 当前 active 幕摘要 + 下一 pending 幕标题"，作为导演脊柱恒定注入（等价于 trpg 的当前节点指引）。
- **地点/势力**：地点命中时（当前场景同名 Location 存在）补充其 Atmosphere/Danger/Points；势力不进常态摘要（经 lore 按需召回）。

### 5.1 与设定库的桥接

编辑器提供"同步到设定库"操作（前端按钮 + 现有 lore upsert API 即可，无需新端点）：把某个角色/物品/地点的长文（背景故事、详细描述）一键生成 `category=character|item|location`、关键词=名称的 lore 条目。**结构化字段管在场注入，lore 管离场召回**，两者互补不重复。

### 5.2 上下文预算与缓存友好排序（已随本期先行落地）

结构化字段会让单轮注入内容变多，配套两项调整（已实施）：

- **上下文预算默认上调**：`context_budget` 默认 6000 → **45000 字符**（按 1 token ≈ 1.5 字符，约 **3 万 tokens**），`lore_budget` 默认 1500 → **4000 字符**。两者均为热更新配置，可在管理后台随时调。注意 `Seed` 只写空键，**已存在的旧库需在配置页手动调一次**（或改库）。
- **prompt 分区稳定前置**：Narrator 为无状态调用（请求 = 系统提示词 + 单条组装消息），DeepSeek 等厂商的前缀缓存按请求前缀分块命中。`BuildNarratorMessage` 的分区顺序改为按"相对稳定 → 每轮剧变"排列，让连续回合的用户消息共享前缀缓存块：

  ```
  规则叙事指导(整局不变) → 游戏上下文/角色卡(基本稳定) → lore front 世界观区
  → 记忆检索块 → 运行态摘要(每轮变) → 场景计划 → 玩家消息(最靠后) → lore tail 风格区
  ```

  裁剪优先级与位置对齐：超预算时优先裁尾部可选分区，保住稳定前缀。lore front 仍在状态摘要之前、tail 仍在玩家消息之后，原有语义不变。

## 六、前端改版

### 6.1 世界编辑器（WorldEditor.vue）

页签调整为：概览 / **主线** / 角色 / **物品** / 地点·势力 / 任务·线索 / 设定库 / **存档** / 注入记录 / JSON。

- **角色页签重写**：抽 `CharacterForm.vue` 组件，每个角色一张折叠卡片，内部分四区：
  - 基本信息：姓名 / 种类 / 角色定位 / 关联人物卡（下拉选卡，显示 CardRef 状态）
  - 形象与性格：外貌（文本域）/ 性格描述（文本域）/ 性格特质（TagsInput）/ 对话风格 / 关键台词
  - 背景与能力：背景故事（文本域）/ 技能（TagsInput）
  - 状态与扮演：立场 / 存活 / 所在地点 / 当前行动 / 动机 / 秘密 / 目标列表 / 情绪 / 备注
  - 每张卡片右上角"从素材库导入"与"同步到设定库"。
- **物品页签**（新）：表格/卡片列表，字段 = 名称/类型/描述/所在地点（下拉，选项来自本世界 Locations）/持有者（下拉，选项来自本世界角色+"玩家"）/标签。
- **主线页签**（新，`storyline` 分区）：标题 + 前提 + 幕列表（标题/摘要/状态 select），支持增删排序；仅 simrpg/roleplay 显示，trpg 显示"主线由剧本时间轴驱动"提示与剧本幕只读预览。
- **地点·势力页签**：地点表单加氛围/危险度/兴趣点；势力加描述/目标/领袖。
- 所有列表页签顶部加"**从素材库导入**"按钮。

### 6.2 素材选择器（AssetPicker.vue，公共组件）

对话框内四栏来源 Tab：

1. **素材库**：按类型/标签/名称过滤全局模板，多选 → 复制导入；支持"编辑后导入"（导入前弹表单微调）。
2. **人物卡**：表格列出全局人物卡（名称/规则体系/玩家），多选 → 关联导入。
3. **其他世界**：先选世界，再按实体类型（角色/地点/物品/势力）树形勾选 → 复制导入。
4. **剧本角色**：先选剧本，勾选角色 → 复制导入。

底部确认栏显示已选清单与冲突处理策略（跳过/覆盖）。

另有独立的**素材库管理页**（`/admin/assets`）：模板的 CRUD、标签维护、从世界收藏的入口汇总。

### 6.3 创建向导改版（WorldsView.vue）

改 `el-steps` 分步（步骤均可跳过非必填项）：

1. **基本信息**：模式 / 世界 ID /（trpg 选剧本）
2. **背景故事**：世界设定大文本 + 设定条目快速录入（现有逐条+段落拆分能力上移到此步）
3. **素材**：AssetPicker 勾选人物卡/剧本角色；逐项添加结构化 NPC（复用 CharacterForm 精简版）、地点、物品、势力
4. **主线剧情**（仅 simrpg/roleplay）：标题/前提/幕列表
5. **确认**：汇总预览 → 创建（一次 POST，扩展后的 worldCreateReq）

创建完成直接跳转世界编辑器继续精修。

## 七、实施分期

| 期 | 内容 | 涉及包 |
|---|---|---|
| P0 | 存储层改造（§九）：世界状态 + 人物卡迁 SQLite（含 saves / asset_library 建表），启动时自动迁移旧 JSON | world, trpg/character, cmd |
| P1 | 数据模型扩展（§三全部）+ 分区 API（items/storyline）+ Prompt 注入更新（§五）+ SeedSpec/worldCreateReq 扩展 | world, web, agent |
| P2 | 素材库 CRUD/收藏 API + 素材目录 + 导入 API（人物卡关联映射、剧本角色复制抽公共函数）+ 存档 API（§9.4） | web, world, trpg/character |
| P3 | 前端：CharacterForm / AssetPicker（四类来源）/ 物品·主线·存档页签 / 素材库管理页 / 地点势力表单细化 / 创建向导分步改版 / Web 聊天页存档入口 | frontend |
| P4 | 验证（go build/vet/test + npm run build + bot.exe 重编）+ 本文档补"实现状态"节 | — |

P0 与 P1 无耦合（实体字段在 JSON 文档内，对存储透明），但先做 P0 可让后续 API 直接建在新存储上。P1→P3 每期结束均可独立编译通过（前端字段缺失时后端 omitempty 兼容，反之亦然）。

## 八、遗留（本期不做，记录在案）

1. **LLM 事件类型扩展**：物品转移（`item_move`）、主线推进（`storyline_advance`）经 ApplyEvent 自动演化——需设计校验规则与 Planner 配合，单独一期。
2. **关系图谱（RelationEdge）可视化编辑**：数据已有，编辑器未做 UI。
3. ~~**独立全局素材库**~~（已提前到本期，见 §9.3：素材库表 + 收藏/导入闭环）。
4. **角色卡数值在编辑器内的直接修改**：人物卡管理页（CharactersView）已有基础编辑，世界编辑器内只做关联不复制数值。
5. **导入冲突的深度合并**（如字段级 merge UI）：本期仅 skip/overwrite 两档。
6. **QQ 端存档命令**（`/存档`、`/读档`）：本期存档只做管理端 + Web 聊天页。

## 九、存储层改造：文件 JSON → SQLite（P0）

### 9.1 判断

读取/关联/素材管理都指向数据库，且条件早已备齐：`StateRepository` 接口预留了 SQLite 实现位；`modernc.org/sqlite`（零 CGO）已是配置库在用的依赖。**采用"元数据列 + JSON 文档"混合存储，不做全关系拆表**——运行时访问模式是"整世界聚合读出→改→写回"，拆表只有 join 重组装成本，没有收益。

### 9.2 方案

- **引擎**：SQLite，与配置库同用 `data/app.db`（单文件备份；多连接设 `busy_timeout`）。
- **表结构**（元数据列冗余自文档，列表/聚合查询不解析大 JSON）：

```sql
CREATE TABLE IF NOT EXISTS worlds (
  id TEXT PRIMARY KEY,
  mode TEXT NOT NULL,
  script_id TEXT,
  script_name TEXT,
  round_count INTEGER DEFAULT 0,
  updated_at TEXT,
  state_json TEXT NOT NULL          -- WorldState 完整 JSON
);
CREATE TABLE IF NOT EXISTS character_cards (
  id TEXT PRIMARY KEY,              -- playerID:charName
  name TEXT NOT NULL,
  player TEXT,
  system TEXT,
  card_json TEXT NOT NULL,
  updated_at TEXT
);
-- 全局素材库：不隶属于任何世界的可复用模板（自由组合的素材池）
CREATE TABLE IF NOT EXISTS asset_library (
  id TEXT PRIMARY KEY,              -- ast_<ulid>
  kind TEXT NOT NULL,               -- character / location / item / faction / storyline
  name TEXT NOT NULL,
  tags TEXT,                        -- JSON 数组，检索用
  summary TEXT,                     -- 一句话摘要（列表展示）
  payload_json TEXT NOT NULL,       -- 对应实体的完整 JSON（CharacterState/Location/Item/...）
  source TEXT,                      -- 来源说明（手动创建 / 收藏自世界X / 剧本Y）
  created_at TEXT,
  updated_at TEXT
);
-- 游玩存档：世界状态的命名快照（一世界多档位）
CREATE TABLE IF NOT EXISTS saves (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  world_id TEXT NOT NULL,
  name TEXT NOT NULL,
  note TEXT,
  mode TEXT,                        -- 冗余：列表展示
  round_count INTEGER,              -- 冗余：列表展示
  auto INTEGER DEFAULT 0,           -- 1=系统自动备份（恢复前/推进前）
  created_at TEXT,
  state_json TEXT NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_saves_world ON saves(world_id);
CREATE INDEX IF NOT EXISTS idx_assets_kind ON asset_library(kind);
```

### 9.3 全局素材库与"自由组合"的关系

不拆表与自由组合不矛盾——组合发生在**导入时复制**，世界文档仍是运行时唯一真相。但"从其他世界复制"只是权宜，本期把原遗留项提前：**全局素材库作为一等公民**。

- **素材来源**：素材库模板 / 全局人物卡 / 其他世界实体 / 剧本角色，四类统一进 AssetPicker。
- **沉淀**：世界编辑器中任何角色/地点/物品/势力/主线可"收藏到素材库"（复制 payload + 打标签）。
- **检索**：素材库列表按 kind + tags + 名称模糊过滤（SQL 直接可查，无需解析 payload）。
- **规模兜底**：若未来跨世界实体检索变重，可加 `entity_index(world_id, kind, name, tags)` 派生索引表（Save 时重建、可随时从 state_json 重算，不构成第二真相）。本期不做。

### 9.4 游玩存档（多档位）

- **语义**：存档 = 某世界某时刻的命名快照；恢复 = 快照整体覆盖当前世界状态（世界锁内执行，Engine 内存缓存同步失效重载）。
- **自动备份**：执行"恢复存档"和"推进时间轴"前，系统自动落一条 `auto=1` 备份档（命名 `自动备份-<时间>`），自动档每世界保留最近 10 条，超出滚动删除。
- **API**（管理端）：`GET|POST /api/admin/worlds/{id}/saves`、`POST /api/admin/worlds/{id}/saves/{sid}/restore`、`DELETE .../saves/{sid}`。恢复/删除均记 note 审计事件。
- **玩家端**：Web 聊天页提供"保存存档 / 读取存档"入口（作用于本会话绑定的世界，与管理端共用 service 层）；QQ 端斜杠命令（`/存档`、`/读档`）列入遗留。
- **前端**：世界编辑器新增"存档"页签（列表：名称/轮次/时间/备注/自动标记；操作：新建/恢复/删除，恢复需二次确认）。

### 9.5 实现与迁移

- **实现**：`world.NewSQLiteRepository(db)` 实现现有 `StateRepository` 接口（Load/Save/Delete/List/Archive 语义不变，Save 内同步元数据列）；人物卡 `character.Manager` 换 SQLite 后端（保留方法签名）。Engine、API、前端零改动。
- **迁移**：启动时检测 `data/worlds/*.json`、`data/characters/*.json`，逐文件导入后改名 `<file>.migrated` 保留备份；幂等（库中已存在同 ID 则跳过不覆盖）。
- **导出**：管理端保留"导出世界 JSON"能力（现有 detail 接口本身即全量 JSON，天然满足人工查看/备份）。

### 9.6 暂不迁移

剧本存档（`data/scripts`）、记忆文件（`data/memories`）本期保持文件存储——低频写、整体读，文件方案无痛点；后续有检索需求再随同样式入库。

## 十、实现状态（2026-08，已全部落地）

> 验证：`go build ./... && go vet ./...` 通过；`go test ./internal/web/ ./internal/world/ ./internal/agent/ ./internal/config/ ./internal/bot/ ./internal/store/ ./pkg/qqbot/` 全 ok；前端 `npm run build` 通过；`bot.exe` 已重编（embed 新 dist）。

### P0 存储层（§九）

- `internal/world/store_sqlite.go`：`OpenSQLite`（busy_timeout + WAL）、`SQLiteRepository`（StateRepository 全接口 + `ListMeta`）、saves 全套（`CreateSave`/`ListSaves`/`LoadSave`/`DeleteSave`，自动档滚动保留 10 条）、`MigrateJSONDir`（旧 worlds JSON 幂等导入 + `.migrated` 留底）。
- `internal/world/asset_store.go`：`AssetStore`（asset_library 表，kind/q/tag 过滤，List 不含 payload）。
- `internal/trpg/character/`：Manager 重构为后端可插拔（`cardBackend` 接口），`NewSQLiteManager` + `MigrateDirToSQLite`；`Card` 新增 `appearance`/`personality`。
- `cmd/bot/main.go`：共享 `data/app.db` 连接，启动时自动迁移旧 JSON；JSONRepository/NewManager 保留（测试与回退用）。
- 修复（顺手）：空 map 经 JSON 往返变 nil 的潜伏问题，`WorldState.ensureMaps()` 统一兜底 Characters/Locations/Factions/Items。

### P1 数据模型与注入（§三/§五）

- `CharacterState` +appearance/personality/backstory/skills；新增 `Item`、`Storyline/StoryAct`；`Faction` +description/goals/leader；`Location` +atmosphere/danger/points；`WorldState` +items/storyline。
- `SeedSpec`/`worldCreateReq` +characters/location_defs/items/factions/storyline/import_cards；`applySpecExtras` 叠加播种（同名角色 `MergeCharacterState` 合并）；roleplay 校验改为 NPCs+Characters 合并计数。
- 分区 API 新增 `items`/`storyline`（storyline 支持 PATCH null 清空）。
- `buildGameStateSummary`：注入导演主线（当前幕+下一幕）、在场/玩家持有物品、同名地点结构化字段、NPC 创作字段（截断防膨胀）；NPC/物品遍历排序输出（保前缀缓存稳定）。
- `CharacterFromScript`（InitFromScript/RefreshScene 去重）与 `CharacterStateFromCard` 公共映射（`internal/world/asset_map.go`）。
- 先行落地：上下文预算默认 45000 字符（≈3 万 token）、lore 预算 4000，prompt 分区稳定前置（§5.2）。

### P2 素材与存档 API（§四/§9.4）

- `internal/web/admin_assets.go`：素材目录 `GET /api/admin/assets`（library/cards/worlds/scripts 四类聚合）；素材库 CRUD + `collect`（世界实体收藏入库）；`import`（四类来源、预校验防半导入、skip/overwrite、note 审计）；存档 list/create/restore/delete（恢复前自动备份，`restoreWorldSave` 共享实现）；推进时间轴前自动备份。
- `internal/web/chat_saves.go`：玩家侧 `/api/saves`（list/create/restore，会话即世界，聊天令牌鉴权）。
- 测试：`world/store_sqlite_test.go`（仓储/存档/素材库）、`web/admin_assets_test.go`（素材全链路 + 存档恢复与自动备份）。

### P3 前端

- 新组件 `CharacterForm.vue`（四区块结构化表单，compact 精简版）、`AssetPicker.vue`（四来源 Tab + 冲突策略）。
- 新页面 `AssetsView.vue`（/admin/assets 素材库管理，按 kind 动态 payload 表单）。
- `WorldEditor.vue`：新增主线/物品/存档页签，角色页签改折叠卡 + CharacterForm + 导入/收藏，地点势力字段补全，JSON_WRITABLE +items/storyline。
- `WorldsView.vue`：创建向导改五步（基本信息→背景故事→素材→主线→确认），地点结构化（location_defs）。
- `ChatView.vue`：顶栏"存档"入口（玩家侧保存/读取）。

### 已知取舍

- 创建向导提交 roleplay 世界时，前端把结构化 NPC/人物卡镜像为 `npcs` 种子（同名走合并，无副作用）。
- "收藏到素材库"读服务端已保存实体，UI 已提示先保存。
- 素材目录 worlds 聚合是逐个 Load 世界（量级小可接受；变大后按 §9.3 加 entity_index 派生表）。

### 遗留（同 §八，不变）

LLM 事件扩展（item_move/storyline_advance）、关系图谱可视化、字段级冲突合并 UI、QQ 端 /存档 /读档 命令、浏览器端人工回归（需真实凭证环境）。

## 十一、模组素材派生与素材解析导入（2026-08 追加）

### 11.1 需求与决策

两个相关诉求：

1. **TRPG 创建世界时模组内容直接带过来**：模组的背景/主线/角色/场景都在剧本里，创建 trpg 世界不应再手动搬运。
2. **素材解析入库**：用户粘贴文本或上传文件（典型：SillyTavern 角色卡，内含性格/背景/世界观），系统自动解析成素材库条目。

关于"解析剧本时是否自动把内容写进素材库"——**决策：不自动入库**。理由：模组素材与剧本是 1:1 强绑定，trpg 世界创建时直接派生即可，入库中转只增加冗余条目；素材库的价值在**跨剧本/跨世界复用**。因此采用两路：

- **直接派生（默认）**：`InitFromScript` 播种时从剧本自动派生主线/地点/势力（角色原本就带），trpg 世界开箱即完整。
- **一键收藏（可选）**：剧本详情提供"收藏到素材库"，把派生素材批量入库并打 `剧本:<剧本名>` 标签 + `来源=剧本《标题》`，供 simrpg/roleplay 等其他世界在 AssetPicker 中按标签筛出、全选引入。重复点击幂等（kind+name+source 去重跳过）。

### 11.2 剧本 → 世界实体派生映射

| 剧本字段 | 派生目标 | 映射 |
|---|---|---|
| `Timeline` 节点 | `Storyline.Acts` | 优先 `type=="act"` 节点；无则取 `is_key_node`；再无则全部节点。首幕 active 其余 pending；Title=剧本标题，Premise=Background.Synopsis |
| `Scenes` | `Locations` | name/description/atmosphere/danger(=danger_level)/points(=investigation_points)/exits |
| `Background.KeyOrganizations` | `Factions` | 仅名称（详情由 lore 条目承载） |
| `Characters` | `Characters` | （原有逻辑，不变） |
| `Background` | `Background` + lore 条目 | （原有逻辑，不变） |

派生在 `InitFromScript` 内执行（QQ 端 / 管理端创建均受益）；`SeedWorld` 的 `applySpecExtras` 仍可在此之上追加/合并（同名角色合并、同名校验按既有规则）。物品不派生——模组线索属于 HiddenInfo/Clues 语义，与可持有物品不同，硬造物品会污染状态。

### 11.3 素材库新增 `world`（世界观）类型

- **payload 结构** `Worldview`：`setting / era / location / atmosphere / tone / backstory / themes[] / lore[]`（lore 为可选的设定条目数组，结构同 `LoreEntry`，导入时重发 ID）。
- **导入世界**：组合各字段为结构化背景文本写入 `state.Background`（`on_conflict=skip` 时若背景非空则跳过；`overwrite` 覆盖），附带 lore 条目追加进 `state.Lore`（按标题去重）。设定条目因此获得"随世界观素材一键迁移"能力。
- **收藏自世界**：`Background` → `Worldview.backstory`，手工条目（source=manual）→ `Worldview.lore`。
- 前端素材库页签与 AssetPicker 同步增加该类型；world 素材的导入入口同时挂在世界编辑器"背景"页签。

### 11.4 素材解析（文本/文件 → 素材草稿）

统一入口 `POST /api/admin/assets/parse`（multipart 文件 或 JSON `{text}`），返回**草稿**（不落库）：`{parser, drafts:[{kind,name,summary,tags,payload}]}`。前端预览/编辑/勾选后经 `POST /api/admin/assets/library/batch` 批量入库——解析与入库解耦，用户始终有确认环节。

两路解析器（自动识别）：

1. **SillyTavern 角色卡（程序化解码 + LLM 整理）**：识别 chara_card_v1/v2/v3 JSON（含 v2/v3 的 `data` 包裹）及 PNG 内嵌卡（手动解析 PNG chunks，取 `tEXt`/`zTXt` 中 `chara`/`ccv3` 关键字的 base64 payload）。卡的 description/personality 常混有卡作者自造的非标准标记（伪 XML tag、方括号块、`{{char}}`/`{{user}}` 宏），无统一语法、无法程序化归类，因此解码后：先机械清理（宏替换、剥 tag 壳、压缩空行，无损保留原文），再将三个自由文本字段交给 LLM 整理，回写到角色 appearance/personality/backstory/skills 及 Worldview 的 setting/era/atmosphere/tone/backstory/themes 对应字段（`Parser.ParseCard`，结果标记 `sillytavern+llm`）；`character_book` 始终程序化直映为 world 草稿的 lore 条目（keys/content，不经 LLM）。LLM 未配置或调用失败时降级为纯程序化直映：`description`→角色 backstory、`personality`→personality、`scenario` 非空→world 草稿 setting（`CardToDrafts`，标记 `sillytavern`）。
2. **自由文本（LLM 单 Agent）**：新包 `internal/assetparse`，复用剧本分析的模型配置（AnalyzerConfig 同款），单 Agent 一次提取 characters/worldview/locations/items/factions/storyline 草稿 JSON。与剧本三阶段流水线的差异：素材文本远短于模组（卡/设定集量级），无需分段索引与逐字保留，单 Agent 足够且快。

`.txt/.md` 文件读取文本走 LLM 路；`.json/.png` 先程序化解码角色卡，不是卡再降级 LLM 自由文本提取。LLM 未配置（Analyzer 为 nil）时角色卡退化为纯程序化直映，自由文本路返回 503。

### 11.5 API 一览（新增）

- `POST /api/admin/scripts/{id}/assets/collect` — 剧本派生素材一键入库（幂等，返回 created/skipped 计数）。
- `POST /api/admin/assets/parse` — 文本/文件解析为素材草稿（不落库）。
- `POST /api/admin/assets/library/batch` — 批量入库（逐项返回成功/失败）。
- 既有 `collect`/`import` 扩展支持 `kind=world`。

### 11.6 实现状态

> 验证：`go build ./... && go vet ./...` 通过；`go test ./internal/web/ ./internal/world/ ./internal/assetparse/ ./internal/agent/ ./internal/config/ ./internal/bot/ ./internal/store/ ./pkg/qqbot/` 全 ok；前端 `npm run build` 通过；`bot.exe` 已重编（embed 新 dist）。

**后端**

- `internal/world/worldview.go`：`Worldview` 结构 + `BuildText`/`ApplyWorldview`（skip/overwrite + lore 按标题去重、重发 ID）/`WorldviewFromState`（收藏：背景 + 手工条目）。
- `internal/world/script_assets.go`：`StorylineFromScript`（act → 关键节点 → 全部节点三级回退，首幕 active）、`LocationsFromScript`、`FactionsFromScript`、`WorldviewFromScript`；`InitFromScript` 自动派生主线/地点/势力（同名不覆盖），trpg 世界创建即完整。
- `internal/world/asset_map.go`：`CharacterFromScript` 补入创作字段（appearance/personality/backstory/skills）。
- `internal/world/asset_store.go`：素材类型白名单新增 `world`。
- `internal/assetparse/`（新包）：`card.go` SillyTavern 卡解析——`DecodeCardJSON`/`DecodeCardPNG` 程序化解码（v1/v2/v3 JSON + PNG tEXt/zTXt/iTXt 内嵌 `chara`/`ccv3`）；`Parser.ParseCard` 混合解析（`cleanCardText` 机械清理宏与伪 tag → LLM 整理 → 回写 CharacterState/Worldview 对应字段，character_book→lore 程序化直映，LLM 失败降级 `CardToDrafts` 纯直映）；`analyzer.go`+`prompt.go` 自由文本单 Agent LLM 提取（六类草稿，输入 6 万字符截断，会话 ID 唯一防串文本，`parseStructured` 供卡解析复用）。
- `internal/web/admin_parse.go`：`POST /api/admin/assets/parse`（multipart/JSON，PNG/JSON 先程序化后降级 LLM，LLM 未配置时 503 明示）、`POST /api/admin/assets/library/batch`（逐项入库不互相阻塞）、`POST /api/admin/scripts/{id}/assets/collect`（五类派生素材一键入库，`剧本:<名>` 标签 + `剧本《标题》` 来源，kind+name+source 幂等去重）。
- `internal/web/admin_assets.go`：collect/import 扩展支持 `kind=world`（导入=写背景+追加 lore；收藏=背景+手工条目）。
- `cmd/bot/main.go`：`assetparse.NewParser`（与剧本识别同模型配置，MaxTokens 8192），注入 `AdminDeps.AssetParser`。

**前端**

- `AssetsView.vue`：world 类型（结构化表单 + lore 只读列表）；"解析导入"对话框（文件/文本二选一 → 草稿分组预览、勾选、改名 → 批量入库，错误透传后端文本）。
- `ScriptsView.vue`：每行"收藏到素材库"按钮（二次确认 + 结果提示）。
- `AssetPicker.vue`：KIND_OPTIONS +world；`WorldsView.vue`：trpg 步骤提示模组素材自动带入。
- `api/admin.js`：`assetApi.parse/parseText/batchCreate`、新增 `scriptApi.collectAssets`。

**测试**：`world/worldview_test.go`、`world/script_assets_test.go`（含 InitFromScript 端到端派生）、`assetparse/card_test.go`（v1/v2/PNG/非卡）、`web/admin_parse_test.go`（剧本收藏幂等、trpg 创建派生、world 素材导入冲突策略、批量入库、SillyTavern JSON/PNG 解析、无 LLM 报错）。

**已知取舍**

- 解析→入库是两步（草稿预览确认），不做"解析即入库"快捷路径。
- world 素材的 lore 条目在素材编辑对话框里只读（可删），精细编辑在导入世界后的设定库页进行。
- LLM 解析是同步请求（素材文本量级小，秒级~几十秒），未复用剧本分析的任务进度推送机制。
- LLM 提取路径需要真实模型验证（测试环境无 LLM key，只覆盖了程序化路径与错误分支）。
