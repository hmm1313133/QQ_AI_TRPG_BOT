# 世界设定库（Lorebook）与按需加载设计

> 版本：v1.0（2026-08-04）
> 范围：管理后台世界管理的结构化编辑重构 + AI 上下文的世界设定按需加载（Lorebook 机制）
> 适用模式：trpg（跑团剧本）/ simrpg（文字 RPG 模拟）/ roleplay（AI 角色扮演）
> 参考：SillyTavern World Info（Lorebook）、AI Dungeon（World Info + Memory + Author's Note）

---

## 一、背景与问题

### 1.1 现状（调研结论）

| 层 | 现状 | 痛点 |
|---|---|---|
| 数据 | 剧本层有结构化 `StoryBackground`（Setting/Era/Location/Atmosphere/Backstory…），但 `InitFromScript` 时被 `BuildStoryContext` **拍平成一大段纯文本**塞进 `WorldState.Background` | 结构化信息永久丢失，之后只能整段编辑、整段注入 |
| AI 注入 | `buildGameStateSummary` 把 **整段 Background + 整段 CampaignSummary + 全量 NPC + 全量 Quests** 无条件拼入上下文，且该分区 required 不可裁剪 | 长背景直接吃光 6000 字符预算；超限时被裁掉的反而是 ScenePlan/角色卡/记忆 |
| 检索 | OpenViking 客户端与双写都在，但 `Find` 路径已废弃，实际检索是本地 bigram 重叠度，且只覆盖记忆条目 | 世界设定/剧本原文完全不进检索，无"按需加载"能力 |
| 管理端 | 世界创建向导=几个大 textarea；详情页**只读**（v-html 拼字符串）；PATCH 端点存在但前端零调用，且只能改 12 种 ApplyEvent 事件 | 用户说的"基本就是一个输入框"；Background/场景描述/任务文本等无任何编辑入口 |
| 编辑范式 | 剧本侧已有 `ScriptEditor.vue` 四页签结构化编辑器 + JSON 兜底 + `KVEditor.vue` | 世界侧没有复用这套已验证的范式 |

### 1.2 目标

1. **设定条目化**：世界背景/设定从"一大段文本"变为"设定库（Lorebook）"——若干条带关键词、分类、优先级的条目（Entry），用户分块填写。
2. **按需加载**：AI 生成时只注入本回合相关的条目（常量条目 + 关键词命中 + 当前场景关联），在 token 预算内按优先级贪心装配。
3. **结构化编辑 UI**：管理后台世界页从只读升级为多页签编辑器，设定库支持条目的增删改、启停、命中测试。
4. **三模式通用**：trpg 剧本导入自动生成条目；simrpg/roleplay 手工维护条目；机制与模式无关。

非目标（本期不做）：多世界共享设定库市场、条目的多人协同编辑、可视化图谱编辑。

---

## 二、竞品机制参考

### 2.1 SillyTavern —— World Info / Lorebook（[官方文档](https://docs.sillytavern.app/usage/core-concepts/worldinfo/)）

最成熟的按需加载方案，核心概念：

- **条目（Entry）**：`keys`（主触发词，多个）+ `content`（注入文本）+ 元数据。
- **触发方式**：
  - **常量（constant）**：不依赖触发词，永远注入（放最核心设定，如一句话世界观）。
  - **关键词触发**：扫描最近 N 条消息（scan depth），命中主键即注入。
  - **次键与选择性逻辑（selective）**：主键命中后还要求次键全部/任一命中（AND）或排除（NOT），用于精确语境（如"龙"只有和"巢穴"同现时才注入龙的生态条目）。
  - **递归扫描（recursive）**：被注入条目的 content 里的词可以再触发其他条目，限制步数防爆炸。
- **预算与排序**：每条目 `insertion_order`（优先级数字），token budget 内高优先级先装，装不下的丢弃；`ignore budget` 条目可豁免。
- **其他旋钮**：大小写敏感、全词匹配、正则键、触发概率、启用/禁用开关、按角色卡内嵌 lorebook。

**借鉴点**：条目模型、常量/触发双通道、主次键逻辑、优先级+预算贪心、递归（限步）、命中测试工具。正则/概率等旋钮本期不做。

### 2.2 AI Dungeon —— World Info + Memory + Author's Note

- **World Info**：与 ST 同源的 keys→entry 机制，但更简陋（无次键、无优先级），实际体验常被诟病"该触发不触发"。
- **Memory（Pinned）**：用户手动钉住的一小段文本，永远注入——对应 ST 的 constant。
- **Author's Note**：插在上下文尾部（靠近生成位置）的风格/语气指引，对输出影响极强。

**借鉴点**：Memory 对应我们的 constant 条目；Author's Note 的"插入位置"概念值得吸收——条目可声明注入到上下文前部（世界观）还是尾部（风格指令，如"保持悬疑语气"）。

### 2.3 Voyage World Engine —— 动态状态 + 验证回环（见《Voyage_World_Engine_Technical_Report.md》）

Voyage 没有 lorebook 概念，它对世界知识的组织思路不同，补充了 ST/AID 都没覆盖的几块：

- **状态与设定分离**：世界知识按"硬事实"类别存结构化状态（角色 HP/技能、物品、地理坐标、任务进度、世界事件），与 LLM 输出解耦——**易变事实不进文本设定**，而是以"事实注入"进 prompt，并有状态锁定（死亡、任务完成不可被 LLM 覆盖）。
- **生成后验证回环**：叙事输出经规则引擎/验证模型检查与状态是否冲突——lorebook 只解决"注入什么"，Voyage 多一步"输出对不对"。
- **四层记忆**：核心身份（永不变）/ 长期 / 短期 / 工作记忆，配"关键事件标记 + 渐进式遗忘（旧记忆权重衰减，关键事件永留）"。
- **向量检索**：Embedding 检索相关历史记忆——按需检索的语义通道，弥补关键词漏召回。
- **离线演化**：玩家下线后世界继续演化，新事实**回写**为状态/设定，否则玩家上线时注入的还是旧世界。
- **管理端完全空白**：Voyage 的设定由平台预置，无用户编辑工具——我们的管理后台正是差异化点。

注意：该报告自述部分实现细节为"合理推测"，借鉴机制即可，不作为实现依据。

### 2.4 结论

以 SillyTavern 条目模型为主干，裁剪掉正则/概率/正则键等长尾旋钮；吸收 AI Dungeon 的"恒定记忆"和"插入位置"两个概念；从 Voyage 吸收三条边界原则——**设定（静态文本条目）与状态（结构化字段）分离、关键词+语义双通道召回、演化结果回写条目**。中文场景下关键词匹配用**子串匹配**（不依赖空格分词，这是与英文 lorebook 最大的实现差异）。

---

## 三、产品策划

### 3.1 核心概念：设定条目（LoreEntry）

用户眼中的一个条目是一张"设定卡片"：

| 字段 | 说明 | 示例 |
|---|---|---|
| 标题 | 卡片名，仅管理用 | 北境·寒鸦堡 |
| 分类 | 背景/地理/势力/人物/物品/规则/历史/风格 八种 | 地理 |
| 关键词 | 触发词列表（chips 输入），命中即注入 | 寒鸦堡、北境长城 |
| 关联词（次键） | 可选；要求与关键词同现（AND）或不得出现（NOT） | AND: 守夜人 |
| 恒定 | 开关：无视关键词永远注入（放最核心设定，少用） | 否 |
| 插入位置 | 前部（世界观区）/ 尾部（风格指令区） | 前部 |
| 优先级 | 0-100，预算不足时低的先被裁 | 60 |
| 启用 | 开关，停用的条目不进检索 | 是 |
| 内容 | 设定正文（textarea） | 寒鸦堡是北境最古老的…… |

**填写引导**（策划要点）：
- 条目粒度建议"一个概念一张卡"（一座城、一个组织、一条规则），编辑器内在分类页签下提示建议粒度。
- **只写静态设定**：地理/势力/规则/历史这类不变或缓变的知识才进条目；HP、任务进度、关系值、在场状态等易变事实永远走 WorldState 结构化字段（事实注入通道），不写进条目——否则条目会过期、与游戏状态打架（Voyage 的"状态与设定分离"原则）。编辑器在条目表单上方固定这句提示。
- 恒定条目建议 ≤ 3 条（世界观一句话、主线一句话、语气一条），UI 上对超过 3 条恒定给出黄色提示。
- 关键词建议 2-5 个，包含别名/俗称；UI 提供"从内容提取候选关键词"按钮（前端简单提取名词性短语，或调一次低成本 LLM——P3 再做，先留按钮位）。

### 3.2 世界编辑器（管理后台页面改版）

`/admin/worlds/{id}` 从只读详情页升级为**多页签编辑器**（复用 `ScriptEditor.vue` 的页签+JSON 兜底范式）：

1. **概览**：世界 ID/模式/当前场景/回合数/指标（只读）+ 危险操作（删除、推进时间轴、归档）。
2. **设定库**（核心页签）：左侧分类树/条目列表（支持搜索、按启用过滤），右侧条目编辑器；顶部"**命中测试**"折叠面板——输入一段测试文本，实时显示哪些条目会被触发、按什么顺序、占多少预算（调后端测试接口）。这是 SillyTavern 用户调试 lorebook 的核心工具，必须有。
3. **场景**：当前场景描述/氛围/危险度/KP 笔记编辑；出口（Exits）行编辑。
4. **角色**：NPC 列表 + 卡片编辑（补全现在无处填的 motivation/secrets/traits/dialogue_style）；按场景归属过滤。
5. **地点/势力**：结构化列表编辑（地点补 Description/Exits，势力补 Description/Goals）。
6. **任务/线索**：Quest 文本、HiddenInfo 编辑。
7. **记忆/事件日志**：沿用现有只读查看（MemoryView 范式）。
8. **JSON**：完整 WorldState 的 JSON 兜底编辑（带校验），高阶用户逃生舱。

创建向导同步升级：三模式的"背景大 textarea"替换为**设定库快速录入**——支持逐条添加，也保留"粘贴大段文本，按段落自动拆成条目草稿"的批量入口（拆分后用户逐条补关键词，降低冷启动成本）。

### 3.3 剧本导入的条目化（trpg 模式）

`InitFromScript` 不再拍平 `StoryBackground`，而是转为条目：

- `Synopsis` + `MainTheme` + `Tone` → 恒定条目（优先级最高，这就是原来的 Background 核心）。
- `Setting/Era/Location/Atmosphere/Backstory` → 各自成条目，关键词从字段名+内容名词生成（如 Location 条目挂地点名）。
- `Timeline` 节点 → 场景条目，关键词 = 节点名 + 关联地点/NPC 名；**默认不恒定**，推进到该节点时由"当前场景关联"通道注入（见 4.3）。
- `Characters` → NPC 条目挂 NPC 名，与角色页签数据同源。

---

## 四、技术方案

### 4.1 数据模型

新增 `internal/world/lore.go`：

```go
// LoreEntry 设定条目（Lorebook 基本单元）
type LoreEntry struct {
    ID          string   `json:"id"`           // lor_xxx，世界内唯一
    Title       string   `json:"title"`
    Category    string   `json:"category"`     // background/geo/faction/character/item/rule/history/style
    Keys        []string `json:"keys"`         // 主触发词（子串匹配，不区分大小写）
    SecondaryKeys []string `json:"secondary_keys,omitempty"` // 次键
    SecondaryMode string `json:"secondary_mode,omitempty"` // and_any / and_all / not_any
    Constant    bool     `json:"constant"`     // 恒定注入
    Position    string   `json:"position"`     // front（世界观区）/ tail（风格指令区）
    Priority    int      `json:"priority"`     // 0-100，预算裁剪依据
    Enabled     bool     `json:"enabled"`
    Content     string   `json:"content"`
    Source      string   `json:"source"`       // manual / script / system（来源，便于筛选与回滚）
}
```

存储：**内嵌进 `WorldState.Lore []LoreEntry`**（沿用每世界单 JSON 文件、`StateRepository` 不动）。理由：条目与世界强绑定（trpg 条目由剧本生成、roleplay 条目即世界本体），跨世界共享设定库是后期需求，届时再加 `data/lorebooks/` 独立库 + `WorldState.LorebookIDs` 引用层即可，接口上先预留 `LoreResolver.Resolve(ws, …)` 以 ws 为入口，未来在 resolver 内部合并共享库，业务侧无感。

兼容迁移：旧世界 `Background` 非空且 `Lore` 为空时，加载后自动把整段 Background 转为一条 constant 条目（`Source: system`），行为与现状完全等价。

### 4.2 检索引擎 LoreResolver

新增 `internal/world/lore_resolver.go`，纯函数、无 LLM、单测覆盖：

```
Resolve(ws *WorldState, scanText string, budget int) (front []LoreEntry, tail []LoreEntry)
```

召回分四层，按序合并去重：

1. **恒定层**：所有 `Constant && Enabled`，无条件入选。
2. **关键词层**：`scanText`（= 本回合玩家消息 + 最近 K 轮对话摘要 + 当前场景名/NPC 名）中对每条目做主键**子串匹配**；命中后检查次键逻辑（and_any/and_all/not_any）。
3. **场景关联层**：当前 Scene 涉及的 NPC/地点名对应的条目（title 或 keys 与场景实体名匹配），弥补"玩家没提关键词但场景相关"的情况——这是现有"全量 NPC 注入"痛点的替代方案。
4. **递归层**（可选，配置开关）：已入选条目的 Content 再跑一次关键词层，最多 2 步，防止链式爆炸。
5. **语义层**（P4+，接口预留）：关键词漏召回时用 embedding 向量检索兜底，与关键词命中共享同一预算池。实现直接站在 trpc-agent-go 的 `knowledge` 包上（见 §4.7），resolver 的召回管线为此预留 `ResolveWithSemantic` 扩展点。

预算装配：入选条目按 Priority 降序贪心装入 lore 分区预算；恒定条目豁免裁剪（对应 ST 的 ignore budget，但靠"建议 ≤3 条"的产品引导约束体积）。

配置项（进 config 注册表，热更新）：

| 键 | 默认 | 说明 |
|---|---|---|
| `lore_budget` | 1500（字符） | lore 分区预算（从 context_budget 中切出） |
| `lore_scan_rounds` | 4 | 关键词扫描的最近轮数 |
| `lore_recursion` | false | 递归扫描开关 |

### 4.3 注入点改造（ContextBuilder）

`internal/agent/context_builder.go` 调整分区：

- 原 `buildGameStateSummary` 拆为两部分：**状态摘要**（场景/在场 NPC/进行中任务/指标——保留 required）与**背景全文**（删除）。
- 新增 **lore 分区**：调 `LoreResolver.Resolve`，front 条目放状态摘要之前（世界观区），tail 条目放玩家消息之后（风格指令区，对应 Author's Note 位置）。
- NPC 注入从"全量"改为"当前场景在场 + lore 命中的角色条目"。
- Planner/Director 调用（turn_engine.go:221）同样改用 resolver 产出设定文本，替代 `state.Background` 全文。
- `CampaignSummary` 保持原样注入（滚动摘要本来就是压缩产物）。

### 4.4 管理后台 API

| 路由 | 说明 |
|---|---|
| `GET /api/admin/worlds/{id}/lore` | 条目列表（可按 category/enabled 过滤） |
| `POST /api/admin/worlds/{id}/lore` | 新增条目 |
| `PUT /api/admin/worlds/{id}/lore/{eid}` | 更新条目 |
| `DELETE /api/admin/worlds/{id}/lore/{eid}` | 删除（script 来源条目允许删，标记 Source 供追溯） |
| `POST /api/admin/worlds/{id}/lore/test` | **命中测试**：body `{text}` → 返回触发条目列表+顺序+预算占用（直接调 LoreResolver） |
| `POST /api/admin/worlds/{id}/lore/import` | 批量导入：粘贴大段文本按段落拆成条目草稿 / 上传 SillyTavern lorebook JSON（字段映射转换，P3） |
| `GET /api/admin/worlds/{id}/lore/injections` | 最近 N 回合的实际注入记录（哪条被触发、命中原因、占多少预算、哪条被裁）——Voyage 式可观测性，用户调关键词/优先级的依据 |
| `GET /api/admin/worlds/{id}/section?part=xxx` | 分区详情（scene/characters/locations/quests…按需返回，替代全量 detail 作为编辑器数据源；全量 detail 保留给 JSON 页签） |

世界字段编辑统一走新增 `PATCH /api/admin/worlds/{id}/section`（body `{part, data}`，服务端按 part 校验后直写对应字段并 Save），不再受限于 ApplyEvent 的 12 种事件——ApplyEvent 仍是**游戏运行时**的唯一写入口，管理端编辑属于**GM 修正规**（记录一条 `note` 事件进 EventLog 便于审计）。

### 4.5 前端实现

- 新增 `views/admin/WorldEditor.vue`（路由 `/admin/worlds/:id`）+ `components/LorebookPanel.vue`（列表+编辑器+命中测试）+ `components/LoreEntryForm.vue`。
- `WorldsView.vue` 列表页保留，"详情"按钮改为跳转编辑器。
- 条目编辑器控件全部用 Element Plus 现成件：`el-select`（分类）、`el-input` 动态 tags（关键词）、`el-switch`（恒定/启用）、`el-slider`（优先级）、`el-radio`（插入位置）。
- 关键词 chips 支持中文逗号/顿号/回车分隔批量录入。
- 命中测试面板：`el-input` + 结果列表，每条显示命中原因（恒定/关键词"寒鸦堡"/场景关联/递归自"北境"）和预算累计条——**命中原因必须展示**，这是可调试性的关键。

### 4.6 分阶段实施

| 阶段 | 内容 | 验收 |
|---|---|---|
| **P1 数据与检索** | LoreEntry 模型 + LoreResolver（恒定/关键词/场景关联/次键，含单测）+ 旧 Background 兼容迁移 + lore_budget 等配置键 | resolver 单测覆盖四层召回与预算裁剪；旧世界加载行为不变 |
| **P2 注入改造** | ContextBuilder 分区改造 + Planner 切换 + NPC 按需注入 | 长背景世界下上下文不超预算；模拟回合验证 lore 条目按预期出现 |
| **P3 管理端** | lore CRUD + test 接口 + section 编辑接口；WorldEditor 多页签 + LorebookPanel + 命中测试面板；创建向导条目化 | 后台可完整编辑世界各分区；命中测试结果与 resolver 单测一致 |
| **P4 剧本联动** | InitFromScript 条目化（StoryBackground/Timeline/Characters → 条目）+ 大段文本拆分导入 + ST lorebook JSON 导入 | 导入剧本后设定库自动填充；老剧本回归 |

另列两项延伸（单独立项，不在本期）："候选关键词 LLM 提取"；**语义召回层**（§4.2 第 5 层，基于 trpc-agent-go `knowledge` 包，见 §4.7）。另有一条运营原则来自 Voyage：simrpg 离线演化（FastForward）产生的新事实应回写为新条目（`Source: system`）或结构化状态，否则玩家上线时注入的还是旧世界——纳入离线演化改造时一并考虑。条目内容的 `{{state.xxx}}` 状态插值也留待该阶段评估。

### 4.7 框架能力映射（trpc-agent-go v1.10.0）

对框架的盘点结论：**`knowledge` 包是本规划最大的可复用资产，multi-agent 编排是未来角色扮演/文字 RPG 的现成底座；关键词触发与预算装配框架没有，自研成本低，维持自研。**

| 规划能力 | 结论 | 框架 package / 关键类型 | 说明 |
|---|---|---|---|
| 关键词触发（恒定/主键/次键） | 自研 | — | 框架无 lorebook 概念；子串匹配逻辑简单，`LoreResolver` 自研 |
| 语义召回（embedding 检索） | **框架直接可用**（持久化需扩展） | `knowledge.NewBuiltinKnowledge` / `vectorstore.VectorStore` / `embedder/openai`（`WithBaseURL` 复用现有 LLM 端点）/ `reranker/topk` / `searchfilter`（条目 category/enabled 过滤） | 接口链完整；`document.Document.Metadata` 天然承载关键词/优先级/来源字段。核心模块只有 inmemory vectorstore（重启丢失），持久化需自实现 `VectorStore`（10 个方法，可落 JSON/SQLite）或引扩展模块 |
| 检索结果注入 agent | 框架可用，项目走手动路径 | `llmagent.WithKnowledge` / `knowledge/tool.NewKnowledgeSearchTool` | 框架支持自动注入和 agentic 工具调用两种模式；但项目 Narrator 是 ContextBuilder 手动装配，直接调 `Knowledge.Search` 拿结果进 lore 分区即可 |
| 上下文预算装配 | 自研（已完成） | 项目 `internal/agent/context_builder.go` | 框架无分区预算装配，保留 ContextBuilder，lore 作为新分区接入 |
| 记忆抽取/反思 | 已用框架（extractor）+ 自研 Reflector | `memory/extractor`、`memory.Service` | 已深度对接；未用的 `ShouldExtract` checker 链、`SearchOption` 高级过滤可后续启用 |
| 会话/世界状态 | 维持自研 | `session.Service` 三级 State | `world.Engine`（时钟/事件队列/EventLog）语义远超通用 session state，迁移收益低 |
| 长会话摘要 | 框架可用（未用） | `session/summary` + `WithSessionSummaryInjectionMode` | Narrator 目前无状态手动装配；若改有状态会话模式可直接启用 |
| 多 agent 编排（角色扮演/文字 RPG） | **框架直接可用** | `chainagent` / `parallelagent` / `cycleagent` / `graphagent`（checkpoint/interrupt）/ `team`（coordinator + swarm） | 多 NPC 分头扮演、多角色 transfer 交接均有现成底座，零额外依赖 |
| 规划器 | 维持自研 | `planner.Planner` / `planner/react` | 框架 planner 是"注入规划指令"模式，与 Director 的低频结构化指令（DecisionDirective + StateUpdates）路数不同 |
| 可观测性 | 框架可用（未用） | `telemetry/metric` / `telemetry/trace` / `telemetry/langfuse` | P3 上线 lore 时顺手接 trace，观察召回质量与装配耗时 |

### 4.8 风险与权衡

1. **中文关键词子串匹配的误伤**："梨"命中"梨花带雨"。缓解：关键词建议≥2字（UI 提示）；次键 NOT 逻辑可排除；命中测试工具让用户提前发现。不做复杂分词库，保持零依赖。
2. **条目质量决定体验**：机制再好，条目写砸了 AI 照样跑飞。缓解：编辑器内嵌填写引导（粒度/关键词数/恒定条数建议），命中测试前置暴露问题。
3. **预算竞争**：lore 分区从 context_budget 切走 1500 字符，记忆/角色卡空间变小。缓解：配置可调；被裁条目在管理端日志可见（TurnEngine 记录每回合注入清单）。
4. **运行时写与管理端写的一致性**：管理端直写字段绕过 ApplyEvent。缓解：统一写后 `Save` + 记 `note` 审计事件；ApplyEvent 仍是回合内唯一写入口的约束不变。

---

## 五、与既有文档的关系

- 落实《管理后台扩展设计.md》遗留的"世界详情只读、PATCH orphan"问题（本文档 §3.2/§4.4）。
- 解决《AI世界引擎技术设计.md》中"背景全量注入"的预算缺陷（本文档 §4.3）。
- 吸收《Voyage_World_Engine_Technical_Report.md》的状态/设定分离、语义召回、演化回写三条原则（本文档 §2.3/§4.2/§4.6）。
- 《待决策事项.md》第 9 项"会话↔世界绑定"不在本文档范围，但 P3 的 section 编辑接口为其预留（世界可先建好再等会话绑定）。

---

## 六、实现状态（2026-08-04，P1–P4 全部落地）

**P1 数据与检索**：`internal/world/lore.go`（LoreEntry + 旧 Background 迁移 helper，幂等）、`lore_resolver.go`（四层召回 + 预算装配，恒定豁免，Reason 可溯）、`lore_resolver_test.go`（11 用例）；配置键 `lore_budget`(1500)/`lore_scan_rounds`(4)/`lore_recursion`(false) 已注册+seed（hot）。

**P2 注入改造**：`buildGameStateSummary` 删除整段 Background（CampaignSummary 保留），NPC 全量→在场+lore 命中（无 Location 信息时退化为全量，保证旧世界行为等价）；ContextBuilder 新增 lore_front（状态摘要前）/lore_tail（玩家消息后）分区；Planner 与 Narrator 同走 resolver；TurnEngine 记录每世界最近 20 回合注入清单（`RecentInjections`）。

**P3 管理端**：后端 `internal/web/admin_lore.go`——lore CRUD / test / injections / import（段落拆分 + SillyTavern lorebook JSON）/ section GET+PATCH（7 分区白名单）；写操作统一记 note 审计事件；创建向导支持 `lore` 数组。前端 `WorldEditor.vue`（`/admin/worlds/:id` 八页签：概览/设定库/场景/角色/地点势力/任务线索/注入记录/JSON 兜底）+ `LorebookPanel.vue`（分类列表+搜索+命中测试面板含预算进度条）+ `LoreEntryForm.vue` + `TagsInput.vue`（中文分隔批量录入）；WorldsView 详情改跳转编辑器，向导支持逐条录入与段落拆分。

**P4 剧本联动**：`internal/world/lore_script.go` `ScriptLoreEntries`——InitFromScript 时 Synopsis 主线→恒定条目（Priority 100），Setting/Era/Location 等各成条目，Timeline 节点→场景条目，Characters→人物条目（均 Source=script）；Background 原文保留兼容旧读路径。

**验证**：`go build/vet` 通过；web/world/agent/config/bot/store/qqbot 七包测试全绿（新增 20+ 用例）；`npm run build` 通过、dist 已更新 embed；`bot.exe` 已重新编译。未做浏览器人工回归。

**遗留**：
1. "从内容提取候选关键词"按钮留位未做（文档原定 P3+ 延伸）。
2. LorebookPanel 面板内未放 ST lorebook 导入入口（`loreApi.importText` 已预留，向导已覆盖文本拆分）。
3. Faction 结构体仅 id/name/reputation，文档 §3.2 的 Description/Goals 待后端加字段。
4. injections 记录无回合号/时间戳（需后端 recordInjection 补字段）。
5. 语义召回层（§4.2 第 5 层，trpc-agent-go `knowledge` 包）与演化回写、`{{state.xxx}}` 插值均未实施，单独立项。
