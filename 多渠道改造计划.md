# 多渠道改造计划（Web 聊天 + 管理后台）

> **版本**: v1.0（计划）
> **日期**: 2026-08-03
> **前置文档**: `AI世界引擎技术设计.md`（引擎架构）、`AI多层架构分析.md`（现状分析）
> **目标**: 将对话能力从 QQ 机器人解耦为多渠道架构，新增 Web 聊天渠道（类 SillyTavern/Voyage 体验）与 Web 管理后台。

---

## 一、背景与目标

### 1.1 现状

- 对话入口只有 QQ 机器人（`internal/bot` + `pkg/qqbot`），通过 WebSocket 收事件、OpenAPI 回消息。
- 管理操作全部依赖聊天指令（`.script load`、`.mode` 等），没有可视化界面。
- 核心调度已经是渠道无关的：`Bot.route()` → `PluginManager.MatchHandler/ChatAgent`，渠道只负责构建 `MessageContext` 和提供 `ReplyFunc`——这是改造的天然接缝。

### 1.2 目标

1. **渠道解耦**：QQ 与 Web 共用同一套 Handler/Agent/世界引擎，消息路由逻辑只写一份。
2. **Web 聊天**：类 SillyTavern 的单页聊天界面，支持完整跑团流程（指令、骰子、剧本、AI 叙事），WebSocket 实时通信。
3. **Web 管理后台**：可视化管理——系统状态、AI 配置、世界/故事进展、剧本、角色卡、记忆、聊天记录。
4. **Web 特有增益**：主动推送（无 QQ 配额限制）、长文本排版、文件上传（剧本 docx/pdf 直接拖拽）。

### 1.3 非目标

- 不做多用户账号体系（自托管单管理员 + 可选访问令牌）。
- 不做移动端 App（Web 响应式即可）。
- 不替换 QQ 渠道（两者并存）。

---

## 二、现状耦合点分析

| 耦合点 | 位置 | 改造方式 |
|--------|------|---------|
| 消息路由逻辑（模式判断→Handler→Agent） | `internal/bot/bot.go` route() | 抽取为 `core.Router`，QQ/Web 共用 |
| QQ 专属字段（Source/OpenID/MsgID/MentionUserID） | `core.MessageContext` | 字段保留（均为 string/int），Web 渠道填自有 ID；Source 枚举加 `SourceWeb` |
| 回复函数 | `core.ReplyFunc` | 已是抽象接口，Web 渠道实现自己的（WS 推送） |
| 附件处理 | `core.Attachment` | 已是通用结构，Web 上传走 HTTP multipart 后填充 |
| 会话模式 | `core.Session.Mode` | 渠道无关，直接复用 |
| 游戏日志 | `gamelog.GameLogger`（Hook 机制） | Hook 挂在 PluginManager 上，渠道无关，直接复用 |
| TimelineEngine 提醒 | session KV 暂存 | Web 渠道可主动 WS 推送（无配额） |

结论：**核心层无需大改**，改造集中在新增渠道层与管理 API 层。

---

## 三、总体架构

```
┌──────────────────────────────────────────────────────────────┐
│ 渠道层 (Channel)                                              │
│  ┌─────────────────┐  ┌────────────────────────────────────┐ │
│  │ QQChannel        │  │ WebChannel                          │ │
│  │ (现有 bot 改造)  │  │  ├─ WS /ws/chat     （聊天实时通道） │ │
│  │                  │  │  ├─ HTTP /api/*     （管理后台 API） │ │
│  │                  │  │  └─ 静态资源 /      （embed 前端）   │ │
│  └────────┬────────┘  └──────────────────┬─────────────────┘ │
│           │         MessageContext        │                   │
│           ▼                               ▼                   │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ core.Router（抽取自 bot.route，渠道共用）                 │ │
│  │   模式判断 → MatchHandler → ExecuteHandler / ChatAgent    │ │
│  └─────────────────────────┬───────────────────────────────┘ │
│                            ▼                                  │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ PluginManager → Handlers / TurnEngine（现有，不变）       │ │
│  └─────────────────────────────────────────────────────────┘ │
├──────────────────────────────────────────────────────────────┤
│ 管理支撑层                                                    │
│  ├─ ConfigStore（运行时配置：JSON 持久化 + 分级热更新）        │
│  └─ AdminAuth（Bearer Token 鉴权中间件）                      │
└──────────────────────────────────────────────────────────────┘
```

部署形态：**单二进制**，QQ 渠道与 Web 渠道可同时启停（`QQ_ENABLED` / `WEB_ENABLED` 环境变量），Web 监听地址在 `conf/trpc_go.yaml` 的 `server.service`（`trpc.trpg.web.Admin`，默认 `0.0.0.0:8080`）中配置。

---

## 四、详细设计

### 4.1 渠道抽象与路由抽取

```go
// internal/core/router.go（新增，逻辑自 bot.go route() 抽取）
type Router struct {
    Plugins  *PluginManager
    Sessions *SessionManager
}
func (r *Router) Route(mc *MessageContext, reply ReplyFunc) error
```

```go
// internal/channel/channel.go（新增）
type Channel interface {
    ID() string
    Start() error
    Stop() error
}
```

- `internal/bot/bot.go` 改造为 `QQChannel`（route 逻辑删除，改调 `core.Router`），行为保持不变（QQ 回归测试保证）。
- `MessageSource` 增加 `SourceWeb`；Web 渠道的 `SessionID = "web:" + 用户令牌`，`UserID = "web:" + 令牌`（单用户）或后续登录用户 ID。
- Web 的 `ReplyFunc`：通过 WS 推送 `{type:"reply", text}`；支持一条回复拆多条推送（长叙事分段）。

### 4.2 Web 聊天通道（WebSocket 协议）

`nhooyr.io/websocket`（已在依赖中，QQ 频道同款库），JSON 消息帧：

```jsonc
// 客户端 → 服务端
{"type": "chat", "text": "我检查一下书桌"}
{"type": "command", "text": ".script load 活神之手"}   // 指令与聊天同通道，Router 自动分流

// 服务端 → 客户端
{"type": "reply", "text": "...", "round": 12}          // AI/指令回复
{"type": "status", "state": "thinking"}                // KP 思考中（收到消息即发，体验优化）
{"type": "push", "text": "..."}                        // 主动推送（TimelineEngine 提醒、世界事件）
{"type": "progress", "stage": "extracting", "text": "..."} // 剧本分析进度（替代 QQ 的逐条回复）
{"type": "error", "text": "..."}
```

要点：
- 聊天消息与指令**同通道复用 core.Router**——`.script` 等全部指令在 Web 端零成本可用。
- 附件上传：HTTP `POST /api/upload`（multipart）→ 存临时文件 → 返回 attachment 描述 → 客户端发 `{"type":"chat","text":".script upload","attachment":{...}}`。
- 断线重连：客户端用本地存储的令牌重连，会话状态在服务端 Session/WorldState 中，天然续跑。

### 4.3 Web 聊天前端（chat.html）

类 SillyTavern 单页布局（vanilla JS + CSS，无构建链，`embed.FS` 内嵌）：

- 消息流（用户/KP 气泡、骰点结果高亮、长叙事排版）
- 输入区（支持指令提示：输入 `.` 弹出可用指令列表）
- 侧栏：当前模式切换、已加载剧本、角色卡摘要、世界状态简览（当前场景/目标清单）
- 顶栏：连接状态、进入管理后台入口

### 4.4 管理后台 API 设计

全部挂载 `/api/admin/*`，Bearer Token 鉴权（`ADMIN_TOKEN` 环境变量）。

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/admin/status` | GET | 版本、运行时长、渠道状态、活跃会话数、LLM 配置摘要（密钥脱敏） |
| `/api/admin/config` | GET / PUT | 运行时配置读写（见 4.6），PUT 返回哪些项立即生效/需重启 |
| `/api/admin/sessions` | GET | 活跃会话列表（渠道、模式、当前剧本、轮次） |
| `/api/admin/worlds` | GET | 世界列表（worldID、模式、剧本、场景、轮次） |
| `/api/admin/worlds/{id}` | GET | WorldState 完整详情（场景/NPC/目标/事件/锁定/指标） |
| `/api/admin/worlds/{id}/advance` | POST | 手动推进时间轴（调 worldEngine.RefreshScene） |
| `/api/admin/worlds/{id}/state` | PATCH | 受控修改（NPC 态度、目标完成、线索发现——经 ApplyEvent 单写入入口） |
| `/api/admin/scripts` | GET | 剧本列表 |
| `/api/admin/scripts/upload` | POST | 上传并分析（复用 analyzeAndSave 流水线，进度轮询 `GET /api/admin/tasks/{taskID}`） |
| `/api/admin/scripts/{id}` | GET / DELETE | 剧本详情 / 删除 |
| `/api/admin/characters` | GET | 角色卡列表（含 NPC 卡） |
| `/api/admin/characters/{id}` | GET / PUT | 角色卡详情 / 属性修改 |
| `/api/admin/memory/{world}/{entity}` | GET | 记忆查看（含重要性、Pinned、失效状态） |
| `/api/admin/logs/{sessionID}` | GET | 聊天记录（复用 gamelog.GetEntries/Export） |
| `/api/admin/tasks/{taskID}` | GET | 异步任务进度（剧本分析等） |

设计原则：
- **一切状态修改经 `world.Engine.ApplyEvent`**（与聊天侧同一写入入口，校验与溯源一致），管理后台不做"特权直写"。
- 异步任务（剧本分析）统一任务表（内存 map + 状态轮询），替代 QQ 的"进度逐条发消息"。

### 4.5 管理前端页面（admin.html）

单页多 Tab（同一套 vanilla JS 技术栈）：

1. **仪表盘**：系统状态、渠道状态、活跃会话/世界卡片
2. **世界管理**：世界列表 → 详情（当前场景、NPC 状态含情绪/态度、目标勾选、待触发事件、锁定事实）→ 手动推进/状态修正
3. **剧本管理**：列表、上传（拖拽 + 进度条）、详情（时间轴/角色/场景树）、删除
4. **AI 配置**：模型、温度（Narrator/Planner）、上下文预算、Plan 间隔、extractor 开关——标注每项"立即生效/重启生效"
5. **角色卡**：列表与编辑
6. **记忆查看器**：按世界/实体浏览记忆条目（重要性/Pinned/失效），支持手动删除
7. **聊天记录**：按会话查看与导出（复用 gamelog）

### 4.6 配置管理（ConfigStore）

现状：配置全部来自启动时环境变量，改配置必须重启改 env。

```go
// internal/config/store.go（新增）
type RuntimeConfig struct {
    LLMModel        string  `json:"llm_model"`
    NarratorTemp    float64 `json:"narrator_temp"`
    DirectorTemp    float64 `json:"director_temp"`
    ContextBudget   int     `json:"context_budget"`
    PlanInterval    int     `json:"plan_interval"`
    ExtractorEnabled bool   `json:"extractor_enabled"`
}
type Store struct { /* JSON 文件 data/config.json + atomic.Value 热读 */ }
```

生效分级：
- **立即生效**（atomic 读取）：温度、上下文预算、Plan 间隔、extractor 开关——TurnEngine/Narrator 每回合读取。
- **重启生效**：模型名、API Key、渠道启停、监听地址——PUT 时明确标注。

优先级：config.json > 环境变量 > 默认值（首次启动用 env 生成 config.json）。

### 4.7 认证与安全

- 管理 API：`Authorization: Bearer <ADMIN_TOKEN>`，未设置 `ADMIN_TOKEN` 时管理后台**仅监听 127.0.0.1**（默认安全配置）。
- 聊天页：可选 `WEB_CHAT_TOKEN`（为空则本机/内网开放）。
- LLM Key、QQ Secret 在 API 输出中一律脱敏（`sk-***`）。
- **安全遗留问题**：`conf/trpc_go.yaml` 当前明文填写了 QQ client_secret，建议迁移到环境变量并从文件中移除（本计划 W5 阶段处理）。
- 上传校验：文件类型白名单（pdf/docx/txt/md）、大小限制（沿用 50MB）。

---

## 五、Web 特有增益

| 能力 | QQ 渠道 | Web 渠道 |
|------|---------|---------|
| 主动推送（定时提醒/世界事件） | 受主动消息月配额限制，当前 session 暂存被动消费 | **WS 直接推送，无配额**——TimelineEngine 在 Web 端真正生效 |
| 长叙事 | 单条消息长度限制，需截断分段 | 不限长，富文本排版 |
| 剧本上传 | 依赖 QQ 文件附件 | 拖拽上传 + 进度条 |
| 思考反馈 | 无（长时间静默） | `status:thinking` 即时反馈 |
| 管理操作 | 纯指令 | 可视化后台 |

---

## 六、与现有代码的映射

| 现有组件 | 改造 |
|---------|------|
| `internal/bot/bot.go` route() | 抽取为 `core.Router`，bot 变为 QQChannel |
| `core.MessageContext` | Source 枚举加 `SourceWeb`，无其他改动 |
| `core.ReplyFunc` / `PluginManager` | 不变（已是渠道无关抽象） |
| `internal/handler/*`（全部指令） | 不变，Web 端经 Router 自动可用 |
| `gamelog` | 不变，管理 API 直接复用 GetEntries/Export |
| `script.Archive` / `ScriptAnalyzer` | 不变，管理 API 包装调用 |
| `world.Engine` | 不变，管理 API 经 ApplyEvent 修改状态 |
| `agent.MemoryService` | 不变，记忆查看 API 读 MemoryStore |
| `cmd/bot/main.go` | 装配 WebChannel + ConfigStore + 管理 API |

---

## 七、分阶段实施

| 阶段 | 内容 | 验收标准 |
|------|------|---------|
| **W1 渠道抽象** | `core.Router` 抽取、Channel 接口、bot 改造为 QQChannel、SourceWeb 枚举 | QQ 端行为零变化（编译+回归测试通过） |
| **W2 Web 聊天** | WebChannel（WS /ws/chat + 静态服务 + 令牌会话）、chat.html 基础聊天、thinking 状态 | 浏览器完成完整跑团回合（指令+AI 叙事） |
| **W3 管理 API** | AdminAuth、status/sessions/worlds/scripts/characters/memory/logs API、任务表 | curl 完成全部管理操作 |
| **W4 管理前端 + 配置** | admin.html 七个 Tab、ConfigStore（分级热更新） | 浏览器完成配置修改与世界管理 |
| **W5 打磨** | WS 主动推送接入 TimelineEngine、上传拖拽、trpc_go.yaml 密钥外迁、响应式细节 | 端到端体验验收 |

每阶段独立可交付，QQ 渠道全程可用。

---

## 八、技术选型

| 项 | 选择 | 理由 |
|----|------|------|
| HTTP 服务 | 优先trpc路由其次标准库 `net/http`（Go 1.25 pattern routing） | 零新依赖，路由够用 |
| WebSocket | `nhooyr.io/websocket`（已在依赖） | QQ 渠道同款，零新依赖 |
| 前端 | vanilla JS + CSS 单页，`embed.FS` 内嵌 | 无 Node 构建链，单二进制分发 |
| 路由/页面 | 两页（chat.html / admin.html）+ 少量 JS 模块 | SillyTavern 式单页足够，不上框架 |
| 认证 | Bearer Token 环境变量 | 自托管单管理员场景的最简方案 |

**零新增第三方依赖**。

---

## 九、风险与缓解

| 风险 | 缓解 |
|------|------|
| 路由抽取破坏 QQ 行为 | W1 单独成阶段，抽取后 QQ 回归验证（指令 + AI 对话） |
| 管理 API 直改状态绕过校验 | 全部经 ApplyEvent 单写入入口，不做特权直写 |
| 公网暴露管理后台 | 未设 ADMIN_TOKEN 时仅监听 127.0.0.1；文档强调反代 + HTTPS |
| WS 连接泄漏 | ping/pong 心跳 + 空闲超时关闭 + 每用户连接数上限 |
| 前端范围蔓延 | 严格限制两页 vanilla JS，不做构建链、不上框架 |
| 配置热更新引发不一致 | 分级生效（立即/重启明确标注），立即生效项用 atomic 读取 |

---

## 十、待确认事项（实现时按此默认值推进）

1. Web 聊天默认**免登录单用户**（自托管），需要访问控制时设 `WEB_CHAT_TOKEN`。
2. 管理后台与聊天共用 trpc_go.yaml 中的服务地址（默认 `:8080`），路径分流（`/ws`、`/api`、静态页）。
3. 配置文件位置 `data/config.json`（与 worlds/memories 同级）。
4. 聊天页风格参考 SillyTavern 的简洁双栏，不做主题系统。

---

*计划完*

---

## 十一、实施记录（W1-W5 已完成，2026-08-03）

### W1 渠道抽象
- `internal/core/router.go`：`Router{Plugins, Sessions, Recorder}` 统一消息路由；`MessageRecorder` 接口解决 core↔gamelog 循环依赖。
- `internal/core/channel.go`：Channel 接口定义；`MessageSource` 增加 `SourceWeb`。
- `internal/bot/bot.go` 重构为 `NewBot(cfg, router)`，QQ 行为不变。

### W2 Web 聊天渠道
- `internal/web/server.go`：会话令牌、`/ws/chat`（nhooyr.io/websocket）、`PushToSession`、静态文件 embed；`buildMux/Handler` 供测试。
- `internal/web/static/chat.html`：简洁现代化聊天页（浅色、圆角卡片、--primary #3b6ef6）。
- `server_test.go` 3 个集成测试通过（含 `.help` 指令 WS 回包）。

### W3 管理后台 API
- `internal/web/admin.go`：`AdminDeps`、`adminAPI`、`wrap` Bearer 鉴权（未设 `ADMIN_TOKEN` 时仅 127.0.0.1 可访问）；端点覆盖状态/会话/世界/剧本（含任务表上传）/角色卡/记忆/日志。
- 配套补充：`character.Manager.ListAll()`、`gamelog.GameLogger.ListSessions()`、`core.SessionManager.List()`、`world.List()/ListWorlds()`。
- 一切世界状态修改经 `world.Engine.ApplyEvent`，管理后台不做特权直写。

### W4 配置存储 + 管理前端
- `internal/config/store.go`：SQLite（modernc.org/sqlite，零 CGO），库文件 `data/app.db`（`CONFIG_DB` 可改）。键：`llm_model`/`narrator_temp`/`director_temp`/`context_budget`/`plan_interval`/`extractor_enabled`；热更新键为后三个。
- **与计划第十节第 3 条的偏差**：运行时配置未用 `data/config.json`，改用 SQLite（用户决策："系统配置采用数据库存储"）。
- 热更新接线：`TurnEngine.SetConfigStore`（每回合读 budget/planInterval）、`MemoryService.SetEnabledFn`。
- `internal/web/static/admin.html`：七 Tab 管理页，与聊天页同设计语言。

### W5 打磨
- `TimelineEngine.SetPushFunc`：idle 提示除写 session KV（QQ 被动消费）外，通过 `web.Server.PushToSession` WS 主动推送到 Web 端（main.go 13c 步接线）。
- `.env.example` 补齐新变量。

### 新增环境变量
| 变量 | 默认 | 说明 |
|------|------|------|
| `WEB_ENABLED` | true | Web 渠道开关 |
| `TRPC_CONF` | ./conf/trpc_go.yaml | trpc-go 配置路径（Web 监听地址在其中 server.service 配置） |
| `WEB_CHAT_TOKEN` | 空 | 聊天页访问令牌（空=不校验） |
| `ADMIN_TOKEN` | 空 | 管理后台 Bearer 令牌（空=仅本机） |
| `CONFIG_DB` | ./data/app.db | 运行时配置 SQLite 路径 |
| `WORLD_DIR` | ./data/worlds | 世界状态目录 |
| `MEMORY_DIR` | ./data/memories | 记忆存储目录 |
| `MEMORY_EXTRACTOR_ENABLED` | true | 记忆抽取开关（可热更新） |

### 关于 conf/trpc_go.yaml 的密钥
QQ secret 等凭证建议统一迁到环境变量（`.env`，已被 .gitignore 忽略），yaml 本体未改动；后续如需完全废弃 trpc 配置加载可再开一项。

### 验证状态
`go build ./...`、`go vet ./...` 通过；web/config/agent/world/store 包测试全绿。QQ 渠道未改动路由逻辑（行为不变）；Web 聊天链路有 httptest 集成测试背书。管理页前端 JS 未在真实浏览器中人工回归，首次使用建议开 `go run ./cmd/bot` 访问 `/chat.html` 与 `/admin.html` 实测。

---

## 十二、W6 前端工程化迁移（2026-08-03）

用户决策：弃用单文件 vanilla 页面，改用框架 + UI 组件库，且"每次编译 Go 前先编译前端"。

- **技术栈**：Vue 3 + Vite + vue-router 4 + Element Plus（JS + SFC，不上 TS）。源码在 `frontend/`，构建产物输出到 `internal/web/static/dist/`（`emptyOutDir`），**dist 需提交**（`go:embed` 依赖，保证 fresh clone 可直接 `go build`）。
- **路由**：`/` → `/chat`（聊天页）；`/admin` 嵌套路由七个子视图（dashboard/worlds/scripts/config/characters/memory/logs）。SPA history 模式。
- **Go 侧**：`internal/web/server.go` 的 `buildMux` 改为 SPA 服务——`GET /assets/` 走 FileServer，`GET /{$}` 重定向 `/chat`，其余 `GET /` 一律回退 `index.html`；WS 处理器限定 `GET /ws/chat` 以消除 mux 模式冲突。旧 `chat.html`/`admin.html` 已删除。
- **功能**：两个页面功能与 API 契约 1:1 保留（WS 协议、上传、六个配置键与热更新标记、任务轮询等）；toast/alert/confirm 升级为 ElMessage/ElMessageBox。
- **Node 工具链**：项目内置便携 Node 于 `tools/node/`（v22.17.0，已 gitignore），无系统 Node 也可构建。
- **构建脚本**：根目录 `build.ps1`——先 `npm install`（首次）+ `npm run build`，再 `go build -o bot.exe ./cmd/bot`；优先用系统 Node，否则回退 tools/node。
- **开发模式**：`frontend/` 下 `npm run dev`，Vite 已配置 `/api`、`/ws` 代理到 `:8080`。
- **验证**：`npm run build` 通过（产物 index.html + assets/）；`go build/vet` 通过；`internal/web` 测试全绿（含 SPA fallback 用例）。页面真实浏览器回归仍建议人工过一次。
