# 待决策事项

> P1-P5 架构改造（2026-08-03）中无法自主确定、留给用户统一处理的事项。
> 每项标注了当前临时方案和涉及代码位置。

---

## 1. 后果传播规则模板（✅ 已于 2026-08-03 解决）

- **现状**：`internal/world/consequence.go` 已实现规则化传播引擎（全英文开发），作为默认实现挂入 `Engine.ApplyEvent`。
- **已实现的规则**：
  - NPC 死亡 → 与死者有紧密关系边（Trust>20 或 ally/family 标签）的 NPC：情绪 valence-40/arousal+20/tag=grieving，对行为方 Trust-50/Fear+30，并写入带 CausedBy 的派生 note 事件（因果链可追溯）
  - NPC 转 hostile → 该 NPC arousal+30/tag=agitated
  - NPC 转 friendly → 对行为方 Trust+10
  - encounter 事件触发 → 所有存活 NPC arousal+20/tag=alert
- **测试**：`internal/world/consequence_test.go` 5 个用例全部通过（含状态锁定拒绝二次死亡的防重放用例）。
- **后续可调**：规则常量（allyTrustThreshold、dealthTrustDelta 等）在 consequence.go 顶部集中定义，可按手感调整。

## 2. 记忆后端升级路径（原 OpenViking 语义检索，已于 2026-08-03 部分解决）

- **现状**：记忆层已迁移到 trpc-agent-go 的 `memory.Service` 抽象：`internal/agent/memory_backend.go` 实现了 JSON 持久化后端（生产默认），对话记忆抽取已接入框架 `extractor`（LLM 抽取 + 去重，`MEMORY_EXTRACTOR_ENABLED=false` 可关闭）。本地 bigram 检索仍是默认相关度方案。
- **待决策**：需要语义检索时的升级路径——接入框架 mem0 后端（需自托管 Mem0 服务）替换 JSON 后端即可，上层代码零改动；OpenViking 是否保留仅用于其他用途。
- **涉及**：`internal/agent/memory_backend.go`、`internal/agent/memory.go`、cmd/bot/main.go

## 3. 便宜模型选型（异步任务降本）

- **现状**：Reflector 反思压缩复用 Director 的 runner（deepseek-chat，temp=0.2）。设计文档要求"便宜模型做粗活"，但未配置独立的经济型模型。
- **待决策**：是否为反思/重要性评分等异步任务配置独立的小模型（如 deepseek-chat 低参数档或本地模型），需要新的 Config 字段与 runner。
- **涉及**：`internal/agent/memory.go` llmReflect、`internal/agent/director.go` rawCompletion

## 4. simrpg / roleplay 模式的启用入口与世界种子

- **现状**：`world.GetMode` 三个模式预设已就绪，`world.Engine.CreateWorld(worldID, mode)` 可创建空世界，TurnEngine 已按模式开关子系统。但**没有用户侧的启用入口**（如 `.world new simrpg` 指令），且 RPG 模式的初始世界内容（地图/NPC/背景）从哪来未定义。
- **待决策**：模式启用 UX（新指令 or 扩展 `.mode`）；RPG 世界种子是玩家描述生成、模板、还是复用剧本解析。
- **涉及**：`internal/world/mode.go`、`internal/agent/turn_engine.go`、`internal/handler/`

## 5. QQ 主动推送策略

- **现状**：TimelineEngine 的 idle 提醒仍写在 session 里等下一条消息消费（被动）。`pkg/qqbot.SendGroupMessage` 支持省略 msg_id 的主动推送，但 QQ 主动消息有月配额。
- **进展（多渠道改造 W5）**：**Web 端已通过 WS 推送解决**——`TimelineEngine.SetPushFunc` 注册后，idle 提示会通过 `web.Server.PushToSession` 即时推送到 Web 聊天页，QQ 端仍保持被动模式。
- **待决策**：QQ 端是否对重要世界事件启用主动推送（消耗配额），还是保持被动模式。
- **涉及**：`internal/trpg/timeline.go`、`pkg/qqbot/message.go`

## 6. 时间映射比例

- **现状**：`world/clock.go` 中现实→世界时间映射为 1:1（`realToWorldRatio`），离线演化阈值为 6 小时（`offlineThresholdMinutes=360`），每回合推进 10 分钟（`DefaultRoundMinutes`）。
- **待决策**：这些数值是否符合世界观预期（如"现实 1 小时 = 世界 1 天"的加速世界）。
- **涉及**：`internal/world/clock.go`

## 7. ContextBuilder 预算与模型分级

- **已决策（2026-08）**：上下文预算默认上调为 45000 字符（`DefaultContextBudget`），约 3 万 tokens（原默认 6000 字符≈4000 tokens）；prompt 分区同步按"稳定→易变"排序以命中厂商前缀缓存（见《世界编辑器与素材联动设计.md》§5.2）。
- **遗留**：预算仍热更新可调；按模型上下文窗口自适应暂不做。
- ~~**待决策**：预算值是否合适；是否需要按模型上下文窗口自适应。~~（见上，已定）
- **涉及**：`internal/agent/context_builder.go`

## 8. EventLog 归档的 LLM 摘要

- **现状**：`JSONRepository.Archive` 是规则化截断（旧事件压缩为一条计数摘要事件）。设计文档设想用 LLM 生成内容摘要。
- **待决策**：归档触发条件（多少轮归档一次）与是否引入 LLM 摘要。
- **涉及**：`internal/world/store.go` Archive

## 9. Director prompt 未列出新事件类型

- **现状**：`update_game_state` 工具已支持 `mood_change` / `relation_change`（ApplyEvent 已实现），但 Director/Planner 的 system prompt 中 state_updates 类型列表未包含这两种新类型，Planner 暂不会主动输出情绪/关系变更。
- **待决策**：是否在 director_prompt.go 的类型列表中补充（需要顺带给出 value 格式说明）。
- **涉及**：`internal/agent/director_prompt.go`

---

*记录完毕。处理时请逐项确认后告知，我来落实。*

## 9. 会话 ↔ 世界绑定机制

- **现状**：世界 ID == 会话 ID。管理后台（2026-08-03 扩展）已可手动创建三模式世界（`Engine.SeedWorld` + `POST /api/admin/worlds`），但自定义 ID 的世界不会被任何聊天会话自动使用；`DELETE /api/admin/worlds/{id}` 的占用检查也只能按 ID 相等判断。
- **待决策**：是否引入 session→worldID 映射（存 session KV 或 ConfigStore 表），TurnEngine 世界解析改走映射（缺省恒等）；以及 simrpg/roleplay 的用户侧入口（新指令 or 扩展 `.mode`）如何与该映射配合。
- **涉及**：`internal/agent/turn_engine.go`、`internal/core/session.go`、`internal/web/admin.go`
