# 管理后台扩展设计

> 2026-08-03 · 基于代码摸底（见文末附录 A）制定。
> 目标：把管理后台从"只读观察 + 配置修改"扩展为覆盖 **QQ 机器人 / 系统配置 / 人物卡 / 剧本 / 世界** 的完整管理面。

---

## 一、总体原则

1. **单写入入口不破**：世界状态的一切修改仍经 `world.Engine`，管理端新增能力全部以 Engine 方法形式实现，不做特权直写 repo。
2. **配置一处注册**：ConfigStore 引入**键注册表（KeyMeta）**，键的中文名、类型、生效级别、是否敏感全部由后端声明，前端动态渲染——消除现在前后端各抄一份 `CONFIG_LABELS`/`HOT_KEYS` 的重复。
3. **敏感值不落明文回包**：密钥类配置（QQ Secret、LLM API Key）读时掩码、写时覆盖，API 永不返回明文。
4. **能不重启进程就不重启**：QQ 凭证修改 → 后台点"重启机器人"即生效；LLM 相关 → 明确标注"重启进程生效"。
5. **复用现有地基**：Manager/Archive/Engine 的 CRUD 大多已存在，本次主要是 HTTP 接线 + 校验 + 前端视图，不重写领域逻辑。

---

## 二、功能设计

### 2.1 QQ 机器人管理（新增 Tab）

**现状**：`bot.Bot` 只有进程级 Start/Stop；WS 层有自动重连但 `connected` 未导出；凭证仅从 env 读一次；`AdminDeps` 拿不到 bot。

**后端**：
- `pkg/qqbot`：WSClient 增加导出状态——`Stats()` 返回 `{Connected bool, LastConnectedAt, ReconnectCount, RxCount, TxCount}`（Rx/Tx 计数器新增，atomic）。
- `internal/bot`：`Bot` 实现新接口（定义在 web 包，避免依赖环）：
  ```go
  type BotController interface {
      Status() BotStatus   // running/connected/stats/启动时间
      Start() error        // 幂等：已运行则 nil
      Stop() error         // 幂等
      Restart() error      // Stop + Start，重读凭证
  }
  ```
  `Bot` 加 `mu sync.Mutex` 保护生命周期；凭证改为 **Start 时从回调读取**（`CredFn func() (appID, secret string)`），main.go 接线读 ConfigStore，从而实现"改凭证 → 重启机器人即生效"。
- 端点（全部走 wrap 鉴权）：
  | 端点 | 说明 |
  |---|---|
  | `GET /api/admin/bot` | 状态：running、connected、uptime、rx/tx、重连次数、当前 AppID（Secret 不回） |
  | `POST /api/admin/bot/start` / `stop` / `restart` | 生命周期控制，幂等 |

**前端**：新 Tab「机器人」——状态卡（连接灯、收发计数、重连次数、运行时长）+ 启停/重启按钮 + 凭证配置入口（链接到系统配置 Tab 的 QQ 分组）。仪表盘加"QQ 渠道"状态卡。

### 2.2 系统配置管理（配置 Tab 重构）

**键注册表**（`internal/config/store.go`）：
```go
type KeyMeta struct {
    Key     string // qq_appid / qq_secret / llm_api_key / llm_base_url / llm_model / ...
    Label   string // 中文名
    Group   string // QQ 机器人 / LLM / 上下文与记忆 / 时间轴
    Type    string // string / number / bool
    Scope   string // hot / bot-restart / process-restart
    Secret  bool   // true: GET 掩码、PUT 空值=不修改
}
```
- 新增键：`qq_appid`、`qq_secret`（secret）、`llm_api_key`（secret）、`llm_base_url`、`web_chat_token`（secret，process-restart）。
- `GET /api/admin/config` 返回 `[]{meta + value}`（secret 值返回 `"********"` 或空）。
- `PUT /api/admin/config` **加白名单校验**：拒绝未注册键；secret 键传空或掩码原样时跳过。
- main.go：所有新键启动时从 env `Seed`（env 优先作为初始值，之后以 DB 为准）。

**前端**：配置页改为按 Group 分组的动态表单（el-form + el-input/el-switch/el-input-number），每项显示生效级别 Tag（绿=立即 / 黄=重启机器人 / 灰=重启进程）；secret 项 placeholder 提示"留空不修改"。

### 2.3 人物卡管理（人物卡 Tab 重构）

**现状**：Manager 全 CRUD 都在，API 只有 List + 部分更新，前端只读。

**后端**：
| 端点 | 说明 |
|---|---|
| `POST /api/admin/characters` | 新建：`{name, player, system, attrs, skills, status, backstory}`，校验 name/system 必填、system ∈ coc7/dnd5e；NPC 卡 player 传 `npc:{scriptID}` |
| `PUT /api/admin/characters/{id}` | 扩展为全字段可改（name/system/backstory + 三个 map 整体替换），保留 ID 不变 |
| `DELETE /api/admin/characters/{id}` | 删除（Manager.Delete 现成） |

**前端**：列表 + 搜索（按名称/玩家过滤）+ 新建/编辑 Drawer（基本信息 + attrs/skills/status 三组键值编辑器：行内增删、数字输入）+ 删除二次确认。

### 2.4 剧本手动创建与编辑（剧本 Tab 扩展）

**现状**：`Archive.Save` 唯一硬校验是 ID 非空；AI 上传链路完整。手动创建最小实用集：`Name/Title/System + Timeline ≥1 节点`。

**后端**：
| 端点 | 说明 |
|---|---|
| `POST /api/admin/scripts` | 接收 Script JSON，服务端：生成 `ID`（复用 analyzer 的 `generateScriptID` 逻辑，提取为公共函数）、补 `CreatedAt`、**结构化校验**（name/title/system 必填；timeline 至少 1 节点；节点 ID 唯一、Order 递增补齐；首节点存在）→ `Archive.Save` |
| `PUT /api/admin/scripts/{id}` | 整体替换，同上校验，ID 不可变 |

**前端**：剧本编辑器（Dialog 或独立路由 `/admin/scripts/edit/:id?`），分四个页签：
1. **基本信息**：Name/Title/System 下拉 + Background 常用字段（Setting/Era/Location/Atmosphere/Synopsis）。
2. **时间轴**：节点列表（拖拽或上下移排序，Order 自动重排），每节点展开编辑 Name/Type/Description/Triggers（逗号分隔）/NPCs/IsKeyNode/KPNotes。
3. **角色**：ScriptCharacter 列表编辑（Name/Role/Personality/Background/Motivation/DialogueStyle）。
4. **JSON**：原始 JSON 编辑 + 校验提示（高级用户兜底，保证结构全覆盖——这是控制工作量的关键：结构化表单只覆盖常用字段，其余走 JSON）。

### 2.5 世界手动创建（世界 Tab 扩展）

**现状**：`Engine.CreateWorld/Delete/InitFromScript` 都在但 CreateWorld 只产空壳且无调用方；simrpg/roleplay 无用户入口。

**后端**：
- `Engine` 新增 `SeedWorld(worldID string, spec SeedSpec) error`：
  ```go
  type SeedSpec struct {
      Mode       string   // trpg / simrpg / roleplay
      Background string   // 世界设定文本
      Scene      string   // 初始场景描述（可选）
      NPCs       []NPCSeed // roleplay 至少 1 个：Name/Kind/Disposition/Personality→Goals
      Locations  []string // simrpg 用
      ScriptID   string   // trpg 专用：改为 CreateWorld + InitFromScript(剧本)
  }
  ```
  在 Engine 内填充 WorldState 并 Save（符合单写入原则：经 Engine 而非 repo）。trpg 模式直接复用 `InitFromScript`。
- 端点：
  | 端点 | 说明 |
  |---|---|
  | `POST /api/admin/worlds` | 创建 + 播种；world_id 可选（默认生成），重复报错 |
  | `DELETE /api/admin/worlds/{id}` | 删除（Engine.Delete 现成；有关联会话活动时拒绝或警告——校验 `Sessions` 里是否有会话正用该 world） |

**前端**：世界列表加"新建世界"按钮，向导式 Dialog：
1. 选模式（三选一，说明各模式差异）；
2. trpg → 选已有剧本；simrpg → 填世界设定 + 初始地点；roleplay → 填背景 + 至少 1 个 NPC（名称/性格/态度）；
3. 确认创建。列表行加"删除"（confirm + 输入 ID 二次确认，防误删）。

**已知限制（如实写入文档，不掩盖）**：世界 ID 与会话的绑定目前是世界 ID == 会话 ID。管理端用自定义 ID 创建的世界**不会自动被任何聊天会话使用**。本阶段价值：trpg 世界预创建/检查/删除 + 为 simrpg/roleplay 打地基。完整的"会话绑定世界"机制（session→worldID 映射 + TurnEngine 解析改造）列入《待决策事项.md》，不在本期范围。

---

## 三、实施阶段

| 阶段 | 内容 | 验证 |
|---|---|---|
| **P1 后端地基** | qqbot Stats + bot BotController + config KeyMeta 注册表 + 全部新端点（bot×4、character×3 改造、script×2、world×2）+ 单测 | build/vet/test 绿；curl 全端点冒烟 |
| **P2 前端视图** | 机器人 Tab、配置页重构（分组动态表单）、人物卡 CRUD、剧本编辑器、世界创建向导 + 路由/nav 更新 | npm run build 过；页面人工回归清单 |
| **P3 收尾** | build.ps1 全链路、文档（本文件标实施记录 + 待决策事项更新 + README 端点清单） | 全量测试 + 构建脚本跑通 |

**范围控制**：不做多管理员/权限分级、不做操作审计日志、不做世界会话绑定（见 2.5 限制）、不动 yaml。这些如需再做另立项。

---

## 附录 A：代码摸底要点（2026-08-03）

- `bot.Bot`（internal/bot/bot.go:56）：无状态字段；Start/Stop 进程级；WS 层（pkg/qqbot/websocket.go:28）有 `connected atomic.Bool` 未导出、3s 重连硬编码。
- ConfigStore（internal/config/store.go）：6 键；HotKeys 3 个；`Set` 无白名单；PUT /api/admin/config 任意键可写（需堵）。
- character.Manager：CRUD 齐全；admin API 只有 List + 三 map 合并更新。
- script.Archive：Save/Get/List/Remove 齐全；Script 结构含 Background/TimelineNode/ScriptCharacter/ScriptScene 全套字段。
- world.Engine：CreateWorld（空壳、无调用方）/Delete/ListWorlds/InitFromScript 均在；`SeedWorld` 需新写；会话绑定世界机制缺失。
- AdminDeps 当前不含 bot；前端 admin 七视图：人物卡只读、剧本无编辑、世界无创建、配置标签前后端重复定义。

---

## 实施记录（P1-P3 已完成，2026-08-03）

### P1 后端地基
- **QQ 机器人**：`pkg/qqbot` 新增 `WSStats`（connected/rx/tx/重连计数/最近连接时间）；`bot.Bot` 加互斥锁实现幂等 Start/Stop/Restart，凭证改为 Start 时经 `CredFn` 从 ConfigStore 读取（改凭证→重启机器人即生效）；`internal/web/admin_bot.go` 定义 `BotController` 接口与 `BotStatus`，端点 `GET /api/admin/bot` + `POST .../start|stop|restart`（未接入 Bot 时 503）。
- **系统配置**：`config.KeyMeta` 注册表（11 键，Key/Label/Group/Type/Scope/Secret）；Scope 三级（hot/bot-restart/process-restart）；GET 返回 meta+值数组、敏感键掩码（`********`）；PUT 白名单拒绝未注册键、敏感键留空或掩码原样跳过。新键：`qq_appid`/`qq_secret`/`llm_api_key`/`llm_base_url`/`web_chat_token`，main.go 启动时从 env Seed。
- **人物卡**：`POST /api/admin/characters`（system 白名单、重复 409）、`DELETE`（404 语义）；PUT 语义定为 **map 传了（含 {}）即整体替换、不传（null）不动**（前端 KVEditor 删键可生效）。
- **剧本**：`script.GenerateScriptID`（analyzer 逻辑提为公共函数）+ `ValidateScript`（必填/节点 ID 唯一/Order 自动补齐）；`POST /api/admin/scripts`（ID 服务端生成、补 CreatedAt、409 冲突）、`PUT /api/admin/scripts/{id}`（整体替换、改名 400）。
- **世界**：`Engine.SeedWorld(worldID, SeedSpec, scr)`（trpg 复用 InitFromScript，simrpg/roleplay 播种背景/场景/NPC/地点；重复不留残态）；`POST /api/admin/worlds`（world_id 缺省 `world_<UnixNano>`，trpg 校验 script_id）、`DELETE /api/admin/worlds/{id}`（会话占用 409）。

### P2 前端视图
- 新增 `BotView.vue`（状态卡 + 启停/重启 + 5s 轮询）、`KVEditor.vue`、`ScriptEditor.vue`（全屏 Dialog 四页签：基本信息/时间轴/角色/JSON 兜底，JSON 切页校验）。
- 配置页重写为分组动态表单（Scope Tag 三色、bool/number/string 控件、secret 留空不提交）；人物卡页重写（搜索/新建 Dialog/编辑 Drawer/删除）；世界页加新建向导（三模式分区表单）与删除（输入 ID 二次确认）；仪表盘加 QQ 渠道卡；导航加「机器人」。
- `adminReq()` 封装：非 2xx 抛带 status 的错误（后端错误为纯文本，409/400 语义依赖它）。

### 顺带修复的预存 bug
- `character.Manager` 把含冒号的卡 ID 直接当文件名，Windows 写入必败——新增 `cardFileName()` 将非法字符替换为 `_`（仅影响磁盘文件名）。
- `NPCSeed`/`SeedSpec` 补显式 JSON tag。

### 与设计的偏差
- `SeedWorld` 签名带 `scr *script.Script` 参数（Engine 不持有 Archive，trpg 剧本由 web 层加载传入）。
- 剧本编辑器采用全屏 Dialog 而非独立路由；编辑模式禁改 name（PUT 要求 ID 一致）。

### 验证
`go build/vet` 通过；web/config/bot/world/agent/qqbot 包测试全绿（新增 bot/config/mgmt/seed 共 20+ 用例）；`npm run build` 通过。`go test ./internal/script/` 全量在本机失败为预存问题（活 LLM 集成测试会读 .env 触发，与本次无关），建议后续加 `testing.Short()` 门控。前端未做浏览器人工回归，建议按清单过一遍：机器人启停、配置保存（含 secret 留空）、角色卡增删改、剧本手动创建+JSON 页签往返、世界三模式创建与删除 409。

---

## 追加：Web 层接入 trpc-go 泛 HTTP（2026-08-04）

- **可行性**：trpc-go v1.0.3 的泛 HTTP 标准服务底层是标准库 `net/http.Server`（`http/transport.go`），支持 Hijack，**WebSocket 可用**。spike 测试（`internal/web/trpc_spike_test.go`）验证 REST + SPA 回退 + WS 升级三链路全通。
- **接入方式（标准化，yaml 注入）**：`internal/web/trpc_server.go` 的 `Server.StartTrpc(confPath)` 使用 `trpc.NewServer()` 标准入口，配置完全由 `conf/trpc_go.yaml` 的 `server.service` 段注入（服务名 `trpc.trpg.web.Admin`，`protocol: http`，`idletime: -1` 防止框架默认 60s 空闲超时回收 WS 长连接），`thttp.RegisterNoProtocolServiceMux` 挂现有 buildMux。`confPath` 由 `TRPC_CONF` 环境变量指定（默认 `./conf/trpc_go.yaml`，等价于框架 `-conf` flag）。框架按约定在配置错误时 panic，`StartTrpc` 内 recover 转为 error 避免拖垮整个进程。
- **原生 net/http 路径已移除**：`web.Config.Addr`、`WEB_ADDR` 环境变量、`Server.Start()`（原生 `http.Server`）均已删除，监听地址/端口统一在 trpc_go.yaml 维护（默认 `0.0.0.0:8080`）；`Stop()` 仅负责关闭 trpc 服务。
- **yaml 适配要点**：`global.enable_set` 必须为字符串 `"N"`（框架 Config 该字段是 string，写 `false` 会解析失败）；`server.filter` 中的名称必须已注册（否则 `trpc.NewServer` panic），本项目未引入 recovery/metrics 插件，故不配置；`server.admin.port` 不配置时 admin 服务自动跳过，不占端口。
- **顺带修复**：管理后台令牌重复弹框 bug——`getAdminToken` 原实现把用户主动留空的令牌存成空串后又被 `|| ''` 判为"未设置"，每次请求都重新弹框；改为仅 `null`（从未设置）才弹框（`frontend/src/api/admin.js`）。
- **验证**：spike 测试改为临时 yaml 注入方式仍全绿；另验证真实 `conf/trpc_go.yaml` 可被 `trpc.LoadConfig` 正确解析（addr=0.0.0.0:8080, idletime=-1）；web/config/bot/world/agent/store/qqbot 包测试全绿；`bot.exe` 已重新编译。
- **后续可利用**（未做）：trpc filter 链做鉴权/日志、admin /cmds 运维接口、metrics 插件体系。
